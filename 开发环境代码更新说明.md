# 开发环境代码更新说明

## 📋 当前服务状态

✅ **后端服务**：正在运行在端口 3000（进程ID: 6572）  
✅ **前端服务**：正在运行在端口 5173（进程ID: 15520）

## 🔄 代码更新机制

### 后端（NestJS）

**自动重启机制**：
- 如果使用 `npm run start:dev` 启动，NestJS 会监听文件变化并**自动重启**
- 修改 `.ts` 文件后，后端会自动重新编译并重启
- 通常需要 **2-5 秒** 完成重启

**如何确认是否在 watch 模式**：
```bash
# 查看后端进程，应该看到类似这样的命令
# nest start --watch
```

**如果代码没有自动更新**：
1. 检查后端是否使用 `start:dev` 启动（而不是 `start`）
2. 手动重启后端：
   ```bash
   cd backend
   npm run start:dev
   ```

### 前端（Vite + React）

**热模块替换（HMR）**：
- Vite 开发服务器支持**热模块替换**
- 修改 `.tsx`、`.ts`、`.css` 文件后，浏览器**自动刷新**或**热更新**
- 通常**立即生效**，无需手动刷新

**如果代码没有自动更新**：
1. 检查浏览器控制台是否有错误
2. 手动刷新浏览器页面（F5 或 Ctrl+R）
3. 如果仍然不行，检查前端服务是否正常运行：
   ```bash
   cd frontend
   npm run dev
   ```

## ⚠️ 重要提示

### 代码已更新，但服务没有变化？

**可能的原因**：

1. **后端代码更改**：
   - ✅ 如果使用 `start:dev`，应该自动重启
   - ❌ 如果使用 `start`，需要手动重启
   - 🔍 查看后端控制台，应该看到类似 `[Nest] 12345  - 01/01/2024, 10:00:00 AM   LOG [NestFactory] Starting Nest application...` 的重启日志

2. **前端代码更改**：
   - ✅ Vite 应该自动热更新
   - 🔍 查看浏览器控制台，应该看到类似 `[vite] hmr update` 的日志
   - 🔄 如果没看到，手动刷新页面

3. **环境变量更改**：
   - ⚠️ 修改 `.env` 文件后，**必须重启服务**才能生效
   - 后端和前端都需要重启

4. **依赖包更改**：
   - ⚠️ 修改 `package.json` 后，需要：
     ```bash
     npm install  # 安装新依赖
     # 然后重启服务
     ```

## 🧪 测试代码是否已更新

### 后端测试

1. **查看后端控制台**：
   - 应该看到最新的日志输出
   - 如果修改了日志，应该立即看到新内容

2. **测试 API 端点**：
   - 访问 `http://localhost:3000/api/upload/diagnose`
   - 应该返回最新的诊断信息

### 前端测试

1. **查看浏览器控制台**：
   - 打开开发者工具（F12）
   - 应该看到最新的 `console.log` 输出
   - 如果修改了日志，应该立即看到新内容

2. **测试功能**：
   - 刷新页面
   - 测试上传功能，查看是否有新的日志输出

## 🔧 手动重启服务

如果自动更新不工作，可以手动重启：

### 重启后端

```bash
# 1. 停止当前服务（在运行后端的终端按 Ctrl+C）
# 2. 重新启动
cd backend
npm run start:dev
```

### 重启前端

```bash
# 1. 停止当前服务（在运行前端的终端按 Ctrl+C）
# 2. 重新启动
cd frontend
npm run dev
```

## 📝 当前修改的文件

以下文件已被修改，应该会自动更新：

1. ✅ `backend/src/upload/upload.service.ts` - 后端服务（需要后端重启）
2. ✅ `frontend/src/pages/AdminDashboard.tsx` - 前端页面（Vite 自动热更新）

## ✅ 验证步骤

1. **后端验证**：
   - 查看后端控制台，应该看到最新的日志
   - 访问诊断端点，查看是否有新的诊断信息

2. **前端验证**：
   - 刷新浏览器页面
   - 打开开发者工具，查看控制台日志
   - 测试上传功能，查看是否有新的日志输出

## 💡 提示

- **后端代码更改**：通常需要等待 2-5 秒自动重启
- **前端代码更改**：通常立即生效（Vite HMR）
- **如果没看到更新**：先检查服务是否正常运行，然后手动刷新/重启

