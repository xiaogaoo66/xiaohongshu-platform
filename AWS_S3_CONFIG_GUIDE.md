# AWS S3 配置指南（详细步骤）

## 为什么需要 S3？

- Base64 方案：每天 200 篇内容 = 12GB/天，数据库会快速增长
- S3 方案：图片存储在云端，数据库只存 URL，体积小，性能好

## 详细配置步骤

### 步骤 1：注册 AWS 账号

1. 访问 https://aws.amazon.com/
2. 点击右上角 "创建 AWS 账户"
3. 填写信息并完成注册（需要信用卡，但免费额度够用）

### 步骤 2：创建 S3 存储桶

1. 登录 AWS Console：https://console.aws.amazon.com/
2. 顶部搜索栏输入 "S3"，点击进入
3. 点击右侧 "创建存储桶" 按钮
4. 填写配置：
   - **存储桶名称**：`xiaohongshu-images-你的用户名`（必须全局唯一）
   - **AWS 区域**：选择 `us-east-1`（最便宜）或 `ap-northeast-1`（东京，适合中国）
   - **对象所有权**：ACL 已禁用（推荐）
   - **阻止所有公共访问**：**取消勾选所有选项**（重要！图片需要公开访问）
   - 其他保持默认
5. 点击 "创建存储桶"

### 步骤 3：配置存储桶权限

1. 进入你刚创建的存储桶
2. 点击 "权限" 标签页
3. 找到 "存储桶策略"，点击 "编辑"
4. 粘贴以下策略（替换 `你的存储桶名称`）：

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadGetObject",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::你的存储桶名称/*"
    }
  ]
}
```

5. 点击 "保存更改"

### 步骤 4：创建 IAM 用户（获取访问密钥）

1. 顶部搜索栏输入 "IAM"，点击进入
2. 左侧菜单点击 "用户"
3. 点击 "创建用户" 按钮
4. 用户名：`xiaohongshu-uploader`
5. 访问类型：勾选 "编程访问"（提供访问密钥）
6. 点击 "下一步：权限"
7. 点击 "直接附加现有策略"
8. 搜索并勾选 `AmazonS3FullAccess`
9. 点击 "下一步" → "创建用户"
10. **重要**：复制并保存以下信息（只显示一次）：
    - **访问密钥 ID**（Access Key ID）
    - **秘密访问密钥**（Secret Access Key）

### 步骤 5：在 Railway 配置环境变量

1. 进入 Railway → 你的后端服务 → Variables 标签
2. 点击 "New Variable" 添加以下变量：

```
AWS_ACCESS_KEY_ID=你的访问密钥ID
AWS_SECRET_ACCESS_KEY=你的秘密访问密钥
AWS_REGION=us-east-1（或你选择的区域）
AWS_S3_BUCKET=你的存储桶名称
```

3. 保存后，Railway 会自动重新部署

### 步骤 6：测试

部署完成后，在前端管理后台上传图片，应该会使用 S3 上传，不再显示 "使用 Base64 编码" 的提示。

## 费用估算

**免费额度（每月）：**
- 存储：5GB
- PUT 请求：2,000 次
- GET 请求：20,000 次
- 数据传输出：1GB

**超出免费额度后的费用（us-east-1）：**
- 存储：$0.023/GB/月（约 ¥0.17/GB/月）
- PUT 请求：$0.005/1,000 次
- GET 请求：$0.0004/1,000 次
- 数据传输：$0.09/GB

**你的使用场景（每天 200 篇）：**
- 每天约 1,800 张图片上传（PUT 请求）
- 每月约 54,000 次 PUT 请求 ≈ $0.27
- 存储 9GB（假设不删除）≈ $0.21/月
- **总计：约 $0.5-1/月（¥3.5-7/月）**

非常便宜！

## 注意事项

1. **存储桶名称必须全局唯一**，如果提示已存在，换个名字
2. **保存好访问密钥**，丢失后需要重新创建
3. **定期清理已领取的内容**，减少存储费用

## 如果遇到问题

- 图片上传失败：检查环境变量是否正确
- 图片无法访问：检查存储桶策略是否配置正确
- 其他问题：查看 Railway 日志，或联系我



