# 🔧 修复 "Failed to fetch" 错误

## 📋 问题原因

"Failed to fetch" 错误表示前端无法连接到后端 API。最常见的原因是：**后端服务未运行**。

---

## ✅ 解决方案

### 步骤 1：启动后端服务

1. **打开新的终端窗口**（保持前端服务运行）

2. **进入后端目录**：
   ```bash
   cd backend
   ```

3. **启动后端服务**：
   ```bash
   npm run start:dev
   ```

4. **等待看到成功提示**：
   ```
   应用运行在端口 3000
   ```

5. **检查是否有错误**：
   - 如果看到数据库连接错误，检查 `DATABASE_URL`
   - 如果看到 S3 警告，说明环境变量已加载但未配置（这是正常的，如果使用 Base64）
   - 如果有其他错误，根据错误信息修复

---

## 🔍 验证修复

### 方法 1：检查后端服务

运行诊断脚本：
```bash
node scripts/check-backend-status.js
```

如果显示 "✅ 后端服务正在运行！"，说明修复成功。

### 方法 2：测试前端

1. 刷新前端页面
2. 尝试上传图片
3. 如果不再显示 "Failed to fetch"，说明修复成功

---

## 📝 常见问题

### 问题 1：后端启动失败

**可能原因**：
- 数据库连接失败
- 环境变量配置错误
- 端口被占用

**解决方案**：
1. 检查 `backend/.env` 文件中的 `DATABASE_URL`
2. 确认数据库服务正在运行
3. 检查端口 3000 是否被占用：
   ```bash
   # Windows
   netstat -ano | findstr :3000
   
   # 如果端口被占用，可以修改 backend/.env 中的 PORT
   ```

### 问题 2：后端启动成功，但前端仍显示错误

**可能原因**：
- 前端代理配置错误
- CORS 问题

**解决方案**：
1. 检查 `frontend/vite.config.ts` 中的 proxy 配置：
   ```typescript
   proxy: {
     '/api': {
       target: 'http://localhost:3000', // 确保这个地址正确
       changeOrigin: true,
     },
   }
   ```

2. 检查浏览器控制台的 Network 标签，查看具体错误信息

3. 确认前端开发服务器正在运行：
   ```bash
   cd frontend
   npm run dev
   ```

### 问题 3：环境变量添加后仍不生效

**重要**：修改 `.env` 文件后，**必须重启后端服务**！

环境变量只在服务启动时加载，修改后需要：
1. 停止后端服务（Ctrl+C）
2. 重新启动：`npm run start:dev`

---

## 🎯 快速检查清单

完成以下检查，确保一切正常：

- [ ] 后端服务正在运行（看到 "应用运行在端口 3000"）
- [ ] 前端开发服务器正在运行
- [ ] `backend/.env` 文件配置正确
- [ ] 数据库服务正在运行
- [ ] 浏览器控制台没有错误
- [ ] 可以正常上传图片

---

## 💡 提示

1. **开发环境需要同时运行两个服务**：
   - 后端：`cd backend && npm run start:dev`（端口 3000）
   - 前端：`cd frontend && npm run dev`（端口 5173）

2. **修改环境变量后必须重启服务**：
   - 修改 `backend/.env` → 重启后端
   - 修改 `frontend/.env` → 重启前端

3. **查看日志**：
   - 后端日志会显示在运行 `npm run start:dev` 的终端
   - 前端日志会显示在浏览器控制台（F12）

---

**最后更新**：2025-01-27

