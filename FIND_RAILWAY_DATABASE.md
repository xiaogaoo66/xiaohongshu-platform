# 🔍 Railway 数据库查找指南

## 📋 问题描述

数据库已存在，但在 Railway 控制台中找不到数据库服务。

---

## ✅ 方法 1：检查环境变量（最快）

如果数据库已存在，`DATABASE_URL` 环境变量应该已经自动配置。

### 1.1 检查后端服务的环境变量

1. 在 Railway 控制台，点击你的后端服务（`xiaohongshu-platform`）
2. 点击 **"Variables"** 标签页
3. 查找 **`DATABASE_URL`** 环境变量

**如果找到了 `DATABASE_URL`：**
- ✅ 数据库确实存在
- 格式类似：`postgresql://postgres:xxx@containers-us-west-xxx.railway.app:5432/railway`
- 说明数据库已创建并连接，只是可能在视图中看不到

**如果没有找到 `DATABASE_URL`：**
- ❌ 数据库可能不存在，需要创建（参考下面的方法）

---

## ✅ 方法 2：查看所有服务列表（推荐）

在 Railway 中，数据库服务可能不在 Architecture 视图中显示，但在服务列表中可以看到。

### 2.1 切换到服务列表视图

1. 在 Railway 项目页面
2. 如果当前在 **"Architecture"** 视图，点击切换到 **"Services"** 或 **"All Services"** 标签
3. 查看所有服务列表

### 2.2 查找数据库服务

在服务列表中查找：
- 名称包含 **"PostgreSQL"** 的服务
- 名称包含 **"Database"** 的服务
- 类型显示为 **"Database"** 的服务
- 图标是数据库图标的服务

---

## ✅ 方法 3：检查其他项目

数据库可能创建在另一个 Railway 项目中。

### 3.1 查看所有项目

1. 在 Railway 控制台左侧，点击 **"Projects"** 或项目列表
2. 检查其他项目
3. 进入每个项目，查看是否有数据库服务

### 3.2 检查共享数据库

如果数据库在另一个项目中：
- 需要将数据库连接到当前项目
- 或者将 `DATABASE_URL` 环境变量复制到当前项目的后端服务

---

## ✅ 方法 4：使用搜索功能

### 4.1 在项目内搜索

1. 在 Railway 项目页面
2. 使用浏览器搜索功能（`Ctrl + F` 或 `Cmd + F`）
3. 搜索关键词：**"PostgreSQL"**、**"Database"**、**"postgres"**

---

## ✅ 方法 5：检查 Architecture 视图设置

### 5.1 调整视图缩放

1. 在 Architecture 视图中
2. 尝试：
   - **缩小视图**（鼠标滚轮向下或 `Ctrl + -`）
   - **拖动视图**（鼠标拖拽空白区域）
   - **重置视图**（查找重置按钮或刷新页面）

数据库服务可能在视图的角落或外部区域。

### 5.2 检查隐藏的服务

1. 在 Architecture 视图中
2. 查看是否有：
   - 被其他服务遮挡的数据库服务
   - 显示在视图边缘的服务
   - 需要滚动才能看到的服务

---

## ✅ 方法 6：通过环境变量确认数据库连接

即使看不到数据库服务，只要环境变量存在，数据库就可以正常使用。

### 6.1 验证数据库连接

1. 在后端服务的 **"Variables"** 中找到 `DATABASE_URL`
2. 复制 `DATABASE_URL` 的值
3. 检查格式是否正确：
   ```
   postgresql://用户名:密码@主机:端口/数据库名
   ```

### 6.2 测试数据库连接

如果后端服务正常运行，说明数据库连接正常：
- 查看后端服务的 **"Logs"** 标签
- 如果看到 "Prisma Client" 或 "Database connected" 等日志
- ✅ 说明数据库连接成功

---

## 🔧 如果确实找不到数据库

### 方案 A：重新创建数据库

1. 在 Architecture 视图中，点击 **"+ Create"** 或 **"+ New"**
2. 选择 **"Database"** → **"Add PostgreSQL"**
3. 等待创建完成（约 1-2 分钟）
4. Railway 会自动生成 `DATABASE_URL` 环境变量

### 方案 B：使用外部数据库

如果不想在 Railway 中创建数据库，可以使用外部 PostgreSQL 数据库：

1. 获取外部数据库的连接字符串
2. 在后端服务的 **"Variables"** 中添加：
   ```
   DATABASE_URL=你的外部数据库连接字符串
   ```

---

## 📝 快速检查清单

按顺序检查：

- [ ] 检查后端服务的 `Variables` 中是否有 `DATABASE_URL`
- [ ] 切换到 `Services` 列表视图查看所有服务
- [ ] 检查其他 Railway 项目
- [ ] 在 Architecture 视图中缩小/拖动查看
- [ ] 查看后端日志确认数据库连接状态
- [ ] 如果都没有，重新创建数据库

---

## 💡 常见情况

### 情况 1：环境变量存在，但看不到服务

**原因**：数据库服务可能被隐藏或不在当前视图范围

**解决**：
- ✅ 只要 `DATABASE_URL` 存在，数据库就可以正常使用
- ✅ 不需要看到数据库服务，只要后端能连接即可

### 情况 2：数据库在另一个项目中

**原因**：创建数据库时选择了错误的项目

**解决**：
- 将 `DATABASE_URL` 从另一个项目复制到当前项目
- 或者将数据库服务移动到当前项目

### 情况 3：数据库服务被删除但环境变量还在

**原因**：数据库服务被误删，但环境变量未清理

**解决**：
- 删除旧的 `DATABASE_URL` 环境变量
- 重新创建数据库服务

---

## 🎯 推荐操作

**如果你确认数据库已存在：**

1. ✅ **检查环境变量**：在后端服务的 `Variables` 中确认 `DATABASE_URL` 存在
2. ✅ **测试连接**：查看后端日志，确认数据库连接正常
3. ✅ **继续使用**：即使看不到数据库服务，只要环境变量存在且后端正常运行，就可以继续使用

**如果你找不到数据库：**

1. ✅ **重新创建**：在 Architecture 视图中创建新的 PostgreSQL 数据库
2. ✅ **等待自动配置**：Railway 会自动生成 `DATABASE_URL` 并添加到所有服务

---

## 📞 需要帮助？

如果以上方法都无法找到数据库，可以：

1. 检查 Railway 账户的 **"Usage"** 页面，查看是否有数据库服务的计费记录
2. 联系 Railway 支持：https://railway.app/help
3. 查看 Railway 文档：https://docs.railway.app/databases/postgresql

