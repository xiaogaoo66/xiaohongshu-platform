# 阿里云 OSS 详细配置指南（完整版）

## 📖 目录

1. [准备工作](#准备工作)
2. [创建 OSS Bucket](#创建-oss-bucket)
3. [配置静态网站托管](#配置静态网站托管)
4. [配置跨域规则（CORS）](#配置跨域规则cors)
5. [配置权限和访问控制](#配置权限和访问控制)
6. [上传前端文件](#上传前端文件)
7. [验证和测试](#验证和测试)
8. [常见配置问题](#常见配置问题)

---

## 准备工作

### 1.1 登录阿里云控制台

1. **访问阿里云官网**
   - 网址：https://www.aliyun.com/
   - 如果没有账号，点击"免费注册"

2. **登录账号**
   - 输入账号和密码登录
   - 如果开启了二次验证，按要求完成验证

3. **进入 OSS 控制台**
   - 登录后，在顶部导航栏找到"产品与服务"
   - 搜索"对象存储 OSS"或直接访问：https://oss.console.aliyun.com/
   - 点击进入 OSS 控制台

### 1.2 获取 AccessKey（用于命令行工具）

如果你计划使用命令行工具（ossutil）上传文件，需要先获取 AccessKey：

1. **进入 RAM 控制台**
   - 访问：https://ram.console.aliyun.com/manage/ak
   - 或通过：阿里云控制台 → 访问控制 → 用户 → 用户 AccessKey

2. **创建 AccessKey**
   - 点击"创建 AccessKey"按钮
   - 按照提示完成身份验证（可能需要手机验证码）
   - **重要**：AccessKey Secret 只显示一次，请立即保存到安全的地方
   - 建议使用记事本或密码管理器保存：
     ```
     AccessKey ID: LTAI5txxxxxxxxxxxxx
     AccessKey Secret: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
     ```

3. **安全建议**
   - ⚠️ 不要将 AccessKey 提交到代码仓库
   - ⚠️ 不要将 AccessKey 分享给他人
   - ⚠️ 定期轮换 AccessKey（建议每 3-6 个月）

---

## 创建 OSS Bucket

### 2.1 进入创建页面

1. 在 OSS 控制台左侧菜单，点击 **"Bucket 列表"**
2. 点击页面右上角的 **"创建 Bucket"** 按钮

### 2.2 配置 Bucket 基本信息

#### 2.2.1 Bucket 名称（必填）

**配置说明**：
- **格式要求**：
  - 只能包含小写字母、数字和连字符（-）
  - 必须以小写字母或数字开头和结尾
  - 长度在 3-63 个字符之间
  - 全局唯一（不能与其他用户重复）

**示例名称**：
```
✅ 正确示例：
- xiaohongshu-frontend-2024
- my-app-frontend-prod
- video-render-frontend

❌ 错误示例：
- Xiaohongshu-Frontend（包含大写字母）
- xiaohongshu_frontend（包含下划线）
- xiaohongshu-frontend-（以连字符结尾）
```

**建议命名规则**：
- 使用项目名称前缀
- 加上用途标识（frontend、assets、static 等）
- 加上环境标识（dev、test、prod）
- 使用连字符分隔

#### 2.2.2 地域（必填）

**选择原则**：
1. **选择离用户最近的地域**，减少访问延迟
2. **选择离后端服务器最近的地域**，减少后端访问延迟

**推荐地域**：
- **华东 1（杭州）**：适合华东地区用户
- **华东 2（上海）**：适合华东地区用户
- **华北 2（北京）**：适合华北地区用户
- **华南 1（深圳）**：适合华南地区用户

**注意事项**：
- ⚠️ 地域一旦选择，**无法修改**
- ⚠️ 不同地域的 OSS 之间数据传输会产生流量费用
- ⚠️ 如果后端也在阿里云，建议选择与后端相同的地域

#### 2.2.3 存储类型（必填）

**选项说明**：
- **标准存储**：✅ 推荐
  - 访问频率高，读写速度快
  - 适合前端静态文件
  - 价格：约 ¥0.12/GB/月

- **低频访问存储**：不推荐
  - 访问频率低，访问时会产生费用
  - 适合备份文件

- **归档存储**：不推荐
  - 需要解冻才能访问
  - 适合长期归档

- **冷归档存储**：不推荐
  - 需要解冻才能访问
  - 适合极长期归档

**选择**：**标准存储**

#### 2.2.4 读写权限（必填）⚠️ 重要

**选项说明**：
- **私有**：❌ 不推荐
  - 只有授权用户可以访问
  - 需要签名才能访问，不适合前端静态文件

- **公共读**：✅ **必须选择**
  - 所有人可以读取，但不能写入
  - 适合前端静态文件托管
  - 浏览器可以直接访问

- **公共读写**：❌ **不推荐**
  - 所有人可以读写，存在安全风险
  - 可能被恶意上传文件

**选择**：**公共读**

**为什么必须选择公共读？**
- 前端静态文件（HTML、JS、CSS）需要被浏览器直接访问
- 如果选择私有，浏览器无法加载这些文件
- 公共读是安全的，因为只能读取，不能写入

#### 2.2.5 同城冗余存储（可选）

**说明**：
- 在同地域内创建多个副本，提高可用性
- 适合对可用性要求极高的场景

**选择**：**关闭**（默认）
- 前端静态文件对可用性要求不是极高
- 标准存储已提供 99.999999999%（11个9）的持久性
- 可以节省成本

#### 2.2.6 版本控制（可选）

**说明**：
- 保留文件的历史版本，可以恢复误删的文件

**选择**：**关闭**（默认）
- 前端文件通常不需要版本控制
- 可以通过 Git 管理代码版本
- 可以节省存储空间

#### 2.2.7 服务器端加密（可选）

**说明**：
- 对存储的文件进行加密

**选择**：**关闭**（默认）
- 前端静态文件通常是公开的，不需要加密
- 可以节省处理时间

#### 2.2.8 实时日志查询（可选）

**说明**：
- 可以查询 OSS 的访问日志，用于分析访问情况

**选择**：**开启**（推荐）
- 方便排查问题
- 可以分析用户访问情况
- 对性能影响很小

#### 2.2.9 标签（可选）

**说明**：
- 为 Bucket 添加标签，方便管理和分类

**示例标签**：
```
标签键：project
标签值：xiaohongshu

标签键：environment
标签值：production
```

**选择**：根据你的需求决定（可选）

### 2.3 确认创建

1. 检查所有配置是否正确
2. 点击页面底部的 **"确定"** 按钮
3. 等待创建完成（通常几秒钟）

### 2.4 记录 Bucket 信息

创建完成后，**立即记录以下信息**（后续会用到）：

```
Bucket 名称：____________________
地域：____________________
OSS 访问域名：____________________
（格式：你的bucket名称.oss-cn-地域.aliyuncs.com）
```

**如何查看 OSS 访问域名**：
- 在 Bucket 列表中，点击你的 Bucket 名称
- 在"概览"页面，可以看到"访问域名"
- 或者直接使用格式：`你的bucket名称.oss-cn-地域.aliyuncs.com`

**示例**：
```
Bucket 名称：xiaohongshu-frontend-2024
地域：华东1（杭州）
OSS 访问域名：xiaohongshu-frontend-2024.oss-cn-hangzhou.aliyuncs.com
```

---

## 配置静态网站托管

### 3.1 为什么需要静态网站托管？

**问题**：
- 直接使用 OSS 访问文件，URL 可能不友好
- React Router 等前端路由，刷新页面会 404

**解决方案**：
- 启用静态网站托管后，OSS 会提供友好的访问地址
- 配置 404 页为 `index.html`，支持前端路由

### 3.2 进入配置页面

1. 在 OSS 控制台，点击你的 Bucket 名称进入
2. 在左侧菜单，找到 **"基础设置"**
3. 点击 **"静态网站托管"**
4. 点击 **"设置"** 按钮

### 3.3 配置参数详解

#### 3.3.1 状态

**选项**：
- **未开启**：默认状态
- **开启**：✅ **必须选择**

**操作**：点击 **"开启"** 开关

#### 3.3.2 默认首页

**作用**：
- 当访问 Bucket 根目录时，返回的文件

**配置**：
```
默认首页：index.html
```

**说明**：
- 这是前端应用的入口文件
- 必须设置为 `index.html`
- 如果文件名不同，填写实际的文件名

#### 3.3.3 默认 404 页 ⚠️ 非常重要

**作用**：
- 当访问的文件不存在时，返回的文件
- **这是支持前端路由的关键配置**

**配置**：
```
默认 404 页：index.html
```

**为什么必须设置为 index.html？**

**问题场景**：
1. 用户访问 `https://frontend.example.com/`
2. 页面正常显示（返回 index.html）
3. 用户点击导航，URL 变为 `https://frontend.example.com/login`
4. 页面正常显示（前端路由工作）
5. **用户刷新页面**（F5 或直接输入 URL）
6. 浏览器请求 `https://frontend.example.com/login`
7. OSS 尝试查找 `login` 文件，但不存在
8. **如果没有配置 404 页，返回 404 错误**
9. **如果配置了 404 页为 index.html，返回 index.html**
10. 前端路由接收到 `index.html`，解析 URL 路径 `/login`，显示正确的页面

**这就是为什么必须设置 404 页为 index.html！**

#### 3.3.4 子目录首页

**作用**：
- 当访问子目录时，返回的文件

**配置**：
```
子目录首页：留空（默认）
```

**说明**：
- 前端应用通常不需要子目录
- 如果留空，子目录会返回 404 或目录列表（取决于配置）

#### 3.3.5 HTTP 重定向

**作用**：
- 将 HTTP 请求重定向到 HTTPS

**配置**：
```
HTTP 重定向：关闭（默认）
```

**说明**：
- OSS 直接访问只支持 HTTP
- 如果需要 HTTPS，需要配置 CDN（见 CDN 配置指南）
- 如果使用 CDN，可以在 CDN 层面配置 HTTPS 重定向

### 3.4 保存配置

1. 检查所有配置是否正确：
   - ✅ 状态：开启
   - ✅ 默认首页：index.html
   - ✅ 默认 404 页：index.html
   - ✅ 子目录首页：留空
   - ✅ HTTP 重定向：关闭

2. 点击页面底部的 **"保存"** 按钮

3. 等待保存完成（通常几秒钟）

### 3.5 获取访问地址

**重要**：静态网站托管配置页面**不会显示**访问地址。需要到 **"概览"** 页面查看。

#### 3.5.1 查看访问地址的方法

**方法 1：在概览页面查看（推荐）**

1. 在 Bucket 详情页面，左侧菜单找到 **"概览"**
2. 在概览页面，找到 **"访问域名"** 或 **"Bucket 域名"** 部分
3. 会显示类似这样的地址：
   ```
   http://你的bucket名称.oss-cn-地域.aliyuncs.com
   ```
4. **记录这个地址**，这就是你的前端访问地址

**方法 2：根据格式自己拼写**

如果你知道 Bucket 名称和地域，可以直接拼写：
```
http://你的bucket名称.oss-cn-地域.aliyuncs.com
```

**地域代码对照表**：
- 华东1（杭州）：`oss-cn-hangzhou`
- 华东2（上海）：`oss-cn-shanghai`
- 华北2（北京）：`oss-cn-beijing`
- 华南1（深圳）：`oss-cn-shenzhen`
- 西南1（成都）：`oss-cn-chengdu`
- 其他地域：在概览页面查看实际的地域代码

**示例**：
```
Bucket 名称：xhs-content
地域：西南1（成都）
访问地址：http://xhs-content.oss-cn-chengdu.aliyuncs.com
```

**注意**：
- ⚠️ 这个地址是 **HTTP** 地址，不是 HTTPS
- ⚠️ 如果需要 HTTPS，需要配置 CDN（见 CDN 配置指南）

---

#### 🤔 重要说明：关于访问地址的常见疑问

**问题 1：文件还没上传，为什么就有访问地址？**

**答案**：
- 这个地址是 **OSS 自动生成的默认域名**，就像给你的存储桶分配的"门牌号"
- 这个地址**和文件是否上传无关**，只要创建了 Bucket 并配置了静态网站托管，就会有这个地址
- 类似于：你租了一个仓库，仓库地址是固定的，不管你放没放东西，地址都存在

**文件还没上传时访问会发生什么？**
- 如果访问根目录（`http://你的bucket名称.oss-cn-地域.aliyuncs.com/`）
  - 会返回 404 错误（因为 `index.html` 还不存在）
  - 或者显示空页面
- **只有上传了 `index.html` 等文件后，访问才会正常显示页面**

**问题 2：我买了域名，在哪里配置？**

**答案**：
- **OSS 本身不支持直接绑定自定义域名**（只能使用默认的 OSS 域名）
- **要使用自定义域名，需要配置 CDN**
- 自定义域名是在 **CDN 控制台**配置的，不是 OSS 控制台

**配置流程**：
```
1. OSS 存储文件（使用默认域名访问）
   ↓
2. 配置 CDN（将你的自定义域名指向 OSS）
   ↓
3. 配置域名解析（将域名指向 CDN）
   ↓
4. 使用自定义域名访问（例如：https://frontend.yourdomain.com）
```

**简单理解**：
- **OSS 默认域名**：`http://你的bucket名称.oss-cn-地域.aliyuncs.com`
  - ✅ 免费使用
  - ❌ 只支持 HTTP（不安全）
  - ❌ 域名不好记
  - ✅ 文件上传后立即可用

- **自定义域名**（通过 CDN）：`https://frontend.yourdomain.com`
  - ✅ 支持 HTTPS（安全）
  - ✅ 域名好记、专业
  - ✅ 访问速度更快（CDN 加速）
  - ❌ 需要配置 CDN（额外步骤）

**建议**：
1. **先上传文件到 OSS**，使用默认域名测试是否正常
2. **确认一切正常后**，再配置 CDN 和自定义域名
3. 这样发现问题时，可以确定是 OSS 的问题还是 CDN 的问题

---

**注意事项**：
- ⚠️ 这是 HTTP 地址，不是 HTTPS
- ⚠️ 如果需要 HTTPS，需要配置 CDN（见 CDN 配置指南）
- ⚠️ 配置可能需要几分钟才能完全生效
- ⚠️ **文件还没上传时，访问会显示 404 或空页面，这是正常的**

---

## 配置跨域规则（CORS）

### 4.1 为什么需要 CORS？

**问题**：
- 浏览器有同源策略限制
- 如果前端和后端不在同一个域名，浏览器会阻止请求
- 需要后端服务器明确允许跨域请求

**说明**：
- OSS 的 CORS 配置主要用于：
  - 允许前端通过 JavaScript 访问 OSS 资源
  - 如果前端需要直接上传文件到 OSS，需要配置 CORS
- **对于前端静态文件托管，CORS 配置不是必须的**（因为浏览器直接访问 HTML/JS/CSS 不受同源策略限制）
- **但建议配置，以防后续需要**

### 4.2 进入配置页面

1. 在 Bucket 详情页面，左侧菜单找到 **"权限管理"**
2. 点击 **"跨域设置"**
3. 点击 **"创建规则"** 按钮

### 4.3 配置参数详解

#### 4.3.1 来源（Origin）

**作用**：
- 指定允许跨域请求的来源域名

**配置选项**：

**选项 1：允许所有来源**（推荐用于开发环境）
```
来源：*
```

**选项 2：允许特定域名**（推荐用于生产环境）
```
来源：https://frontend.yourdomain.com
```

**选项 3：允许多个域名**（如果需要）
```
来源：
https://frontend.yourdomain.com
https://www.yourdomain.com
http://localhost:5173
```

**说明**：
- 如果使用 `*`，允许所有来源（开发环境方便）
- 如果使用具体域名，更安全（生产环境推荐）
- 可以填写多个域名，每行一个
- **不需要包含协议**（http/https），但填了也没问题

**推荐配置**：
- 开发环境：`*`
- 生产环境：填写你的前端域名

#### 4.3.2 允许 Methods

**作用**：
- 指定允许的 HTTP 方法

**配置**：
```
允许 Methods：
☑ GET
☑ HEAD
☐ POST
☐ PUT
☐ DELETE
```

**说明**：
- **GET**：读取文件（必须）
- **HEAD**：获取文件头信息（必须）
- **POST**：上传文件（如果前端需要直接上传到 OSS）
- **PUT**：更新文件（如果前端需要直接更新文件）
- **DELETE**：删除文件（如果前端需要直接删除文件）

**前端静态文件托管**：
- 只需要 GET 和 HEAD
- 如果后续需要前端直接上传文件，可以添加 POST

#### 4.3.3 允许 Headers

**作用**：
- 指定允许的请求头

**配置选项**：

**选项 1：允许所有请求头**（推荐）
```
允许 Headers：*
```

**选项 2：允许特定请求头**
```
允许 Headers：
Content-Type
Authorization
x-oss-*
```

**说明**：
- 如果使用 `*`，允许所有请求头（推荐，简单）
- 如果需要更严格的安全控制，可以指定具体请求头
- 多个请求头用逗号分隔

**推荐配置**：`*`

#### 4.3.4 暴露 Headers

**作用**：
- 指定浏览器可以访问的响应头

**配置**：
```
暴露 Headers：
ETag
x-oss-request-id
Content-Length
```

**说明**：
- **ETag**：文件的唯一标识，用于缓存验证（推荐）
- **x-oss-request-id**：请求 ID，用于排查问题（推荐）
- **Content-Length**：文件大小（可选）
- 多个响应头用逗号分隔

**推荐配置**：
```
ETag, x-oss-request-id
```

#### 4.3.5 缓存时间

**作用**：
- 指定浏览器缓存 CORS 预检请求结果的时间（秒）

**配置**：
```
缓存时间：0
```

**说明**：
- **0**：不缓存，每次都发送预检请求（开发环境推荐）
- **3600**：缓存 1 小时（生产环境可以设置）
- 预检请求是浏览器在发送实际请求前，先发送 OPTIONS 请求询问是否允许跨域

**推荐配置**：
- 开发环境：`0`
- 生产环境：`3600`（1小时）

### 4.4 保存配置

1. 检查所有配置：
   - ✅ 来源：`*` 或你的前端域名
   - ✅ 允许 Methods：GET, HEAD
   - ✅ 允许 Headers：`*`
   - ✅ 暴露 Headers：ETag, x-oss-request-id
   - ✅ 缓存时间：0 或 3600

2. 点击页面底部的 **"确定"** 按钮

3. 等待保存完成

### 4.5 配置多个规则（可选）

如果你需要为不同的场景配置不同的规则，可以创建多个规则：

**规则 1：开发环境**
```
来源：*
允许 Methods：GET, HEAD, POST
允许 Headers：*
暴露 Headers：ETag, x-oss-request-id
缓存时间：0
```

**规则 2：生产环境**
```
来源：https://frontend.yourdomain.com
允许 Methods：GET, HEAD
允许 Headers：*
暴露 Headers：ETag, x-oss-request-id
缓存时间：3600
```

---

## 配置权限和访问控制

### 5.1 检查 Bucket 权限

1. 在 Bucket 详情页面，左侧菜单找到 **"权限管理"**
2. 点击 **"访问权限"**

3. **确认权限设置**：
   - ✅ 应该显示：**公共读**
   - 如果不是，需要修改（见下方）

### 5.2 修改 Bucket 权限（如果需要）

如果权限不是"公共读"，需要修改：

1. 在"访问权限"页面，点击 **"修改"** 按钮
2. 选择 **"公共读"**
3. 确认修改
4. ⚠️ 注意：修改权限可能需要几分钟生效

### 5.3 配置防盗链（可选，推荐）

**作用**：
- 防止其他网站直接链接你的资源（消耗你的流量）
- 只允许指定的域名访问

**配置步骤**：

1. 在"权限管理"页面，点击 **"防盗链"**
2. 点击 **"设置"** 按钮
3. 配置参数：
   ```
   启用 Referer：开启
   
   允许空 Referer：关闭（推荐）
   （说明：如果开启，允许直接访问；如果关闭，必须通过指定域名访问）
   
   白名单：
   https://frontend.yourdomain.com
   https://www.yourdomain.com
   ```

4. **注意事项**：
   - ⚠️ 如果配置了防盗链，只有白名单中的域名可以访问
   - ⚠️ 如果关闭"允许空 Referer"，直接访问 OSS 地址会被拒绝
   - ⚠️ 开发环境建议关闭防盗链，或开启"允许空 Referer"
   - ⚠️ 生产环境建议开启防盗链，保护资源

**推荐配置**：
- 开发环境：不开启防盗链
- 生产环境：开启防盗链，添加你的前端域名到白名单

---

## 上传前端文件

### 6.1 本地构建前端

在开始上传之前，需要先构建前端项目：

```bash
# 进入前端目录
cd frontend

# 安装依赖（如果还没有安装）
npm install

# 构建项目
npm run build
```

**构建完成后**：
- 构建产物在 `frontend/dist` 目录
- 包含 `index.html` 和 `assets` 文件夹

**检查构建产物**：
```bash
# 查看构建产物
ls frontend/dist
# 应该看到：
# - index.html
# - assets/
#   - index-xxxxx.js
#   - index-xxxxx.css
#   - ...
```

### 6.2 方法 1：使用阿里云控制台上传（最简单）

#### 6.2.1 进入上传页面

1. 在 OSS 控制台，进入你的 Bucket
2. 点击左侧菜单 **"文件管理"**
3. 点击页面右上角的 **"上传文件"** 按钮

#### 6.2.2 选择文件

**重要**：只上传 `dist` 目录**内部的文件**，不要上传 `dist` 文件夹本身！

**正确的文件结构**：
```
OSS Bucket 根目录：
├── index.html          ✅ 正确
├── assets/             ✅ 正确
│   ├── index-xxxxx.js
│   ├── index-xxxxx.css
│   └── ...
```

**错误的文件结构**：
```
OSS Bucket 根目录：
└── dist/               ❌ 错误！
    ├── index.html
    └── assets/
```

**操作步骤**：

1. **方式 A：选择文件**
   - 点击 **"选择文件"** 按钮
   - 在文件选择器中，进入 `frontend/dist` 目录
   - 选择 `index.html` 文件
   - 按住 Ctrl（Windows）或 Cmd（Mac），继续选择 `assets` 文件夹
   - 点击"打开"

2. **方式 B：拖拽上传**
   - 打开文件管理器，进入 `frontend/dist` 目录
   - 选择所有文件和文件夹（`index.html` 和 `assets` 文件夹）
   - 拖拽到浏览器窗口的上传区域

#### 6.2.3 配置上传选项

**存储类型**：
- 选择：**标准存储**（默认）

**上传路径**：
- 留空（默认上传到 Bucket 根目录）
- ⚠️ 不要填写 `dist/` 或其他路径

**覆盖同名文件**：
- ✅ 勾选（如果之前上传过，需要覆盖）

#### 6.2.4 开始上传

1. 检查文件列表：
   - 应该看到 `index.html` 和 `assets` 文件夹
   - 文件数量应该和 `dist` 目录中的文件数量一致

2. 点击 **"上传"** 按钮

3. 等待上传完成：
   - 上传进度会显示在页面上
   - 上传时间取决于文件大小和网络速度
   - 通常几秒到几分钟

#### 6.2.5 验证上传

1. 上传完成后，在"文件管理"页面，应该能看到：
   ```
   文件列表：
   ├── index.html
   └── assets/
       ├── index-xxxxx.js
       ├── index-xxxxx.css
       └── ...
   ```

2. **检查文件路径**：
   - `index.html` 应该在根目录（路径显示为 `index.html`）
   - `assets` 文件夹应该在根目录（路径显示为 `assets/`）

3. **检查文件大小**：
   - 文件大小应该和本地 `dist` 目录中的文件大小一致

### 6.3 方法 2：使用 ossutil 命令行工具（推荐，适合频繁更新）

#### 6.3.1 下载和安装 ossutil

**Windows**：

1. **方式 A：使用 Chocolatey**（如果有）
   ```powershell
   choco install ossutil
   ```

2. **方式 B：手动下载**
   - 访问：https://help.aliyun.com/document_detail/120075.html
   - 下载 Windows 版本
   - 解压到任意目录（例如 `C:\ossutil`）
   - 将目录添加到系统 PATH 环境变量

**Mac**：

```bash
# 使用 Homebrew
brew install ossutil

# 或手动下载
# 访问：https://help.aliyun.com/document_detail/120075.html
```

**Linux**：

```bash
# 下载
wget http://gosspublic.alicdn.com/ossutil/1.7.14/ossutil64

# 添加执行权限
chmod 755 ossutil64

# 移动到系统路径
sudo mv ossutil64 /usr/local/bin/ossutil
```

#### 6.3.2 配置 ossutil

```bash
ossutil config
```

**配置参数**：

```
请输入配置文件路径：直接回车（使用默认路径）
   （Windows 默认：C:\Users\你的用户名\.ossutilconfig）
   （Mac/Linux 默认：~/.ossutilconfig）

请输入 endpoint：oss-cn-hangzhou.aliyuncs.com
   （根据你的 Bucket 地域修改：
    - 华东1-杭州：oss-cn-hangzhou.aliyuncs.com
    - 华东2-上海：oss-cn-shanghai.aliyuncs.com
    - 华北2-北京：oss-cn-beijing.aliyuncs.com
    - 华南1-深圳：oss-cn-shenzhen.aliyuncs.com
    - 其他地域：查看 OSS 文档）

请输入 accessKeyId：LTAI5txxxxxxxxxxxxx
   （你的 AccessKey ID）

请输入 accessKeySecret：xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   （你的 AccessKey Secret）

请输入 stsToken：直接回车（不使用 STS）
```

**配置完成后**：
- 配置文件保存在 `~/.ossutilconfig`（Mac/Linux）或 `C:\Users\你的用户名\.ossutilconfig`（Windows）
- 后续使用不需要再配置

#### 6.3.3 测试配置

```bash
# 列出 Bucket 列表，测试配置是否正确
ossutil ls

# 如果配置正确，应该看到你的 Bucket 列表
```

#### 6.3.4 上传文件

```bash
# 进入前端目录
cd frontend

# 上传 dist 目录所有内容到 OSS
ossutil cp -r dist/ oss://你的bucket名称/ --update

# 参数说明：
# -r：递归上传目录
# dist/：本地目录（注意最后的斜杠）
# oss://你的bucket名称/：OSS 路径（注意最后的斜杠）
# --update：只上传修改过的文件（提高速度）

# 示例：
# ossutil cp -r dist/ oss://xiaohongshu-frontend-2024/ --update
```

**上传选项**：

```bash
# 强制上传所有文件（即使文件没有变化）
ossutil cp -r dist/ oss://你的bucket名称/ --update --force

# 上传时显示详细信息
ossutil cp -r dist/ oss://你的bucket名称/ --update --output-dir=./upload-log

# 只上传特定文件类型
ossutil cp -r dist/ oss://你的bucket名称/ --update --include "*.html,*.js,*.css"
```

#### 6.3.5 验证上传

```bash
# 列出 OSS 中的文件
ossutil ls oss://你的bucket名称/

# 应该看到：
# oss://你的bucket名称/index.html
# oss://你的bucket名称/assets/
```

### 6.4 方法 3：使用 OSS Browser（图形化工具）

#### 6.4.1 下载 OSS Browser

1. 访问：https://help.aliyun.com/document_detail/61872.html
2. 下载对应系统的版本：
   - Windows：下载 Windows 版本
   - Mac：下载 Mac 版本
   - Linux：下载 Linux 版本

#### 6.4.2 安装和登录

1. 解压下载的文件
2. 运行 OSS Browser
3. 使用阿里云账号登录：
   - 点击"使用阿里云账号登录"
   - 按照提示完成登录

#### 6.4.3 选择 Bucket

1. 登录后，在左侧 Bucket 列表中找到你的 Bucket
2. 点击 Bucket 名称进入

#### 6.4.4 上传文件

1. 在本地文件管理器中，进入 `frontend/dist` 目录
2. 选择所有文件（`index.html` 和 `assets` 文件夹）
3. 拖拽到 OSS Browser 窗口
4. 或者点击"上传"按钮，选择文件

#### 6.4.5 验证上传

上传完成后，在 OSS Browser 中应该能看到：
- `index.html` 在根目录
- `assets` 文件夹包含所有 JS、CSS 文件

---

## 验证和测试

### 7.1 测试 1：访问 OSS 直接地址

1. **获取访问地址**：
   ```
   http://你的bucket名称.oss-cn-地域.aliyuncs.com
   ```

2. **在浏览器中访问**：
   - 打开浏览器（Chrome、Firefox、Edge 等）
   - 在地址栏输入访问地址
   - 按回车访问

3. **预期结果**：
   - ✅ 页面正常加载
   - ✅ 显示前端应用界面
   - ✅ 没有 404 错误
   - ✅ 样式和脚本正常加载
   - ✅ 控制台没有错误（F12 打开开发者工具检查）

4. **如果页面空白**：
   - 打开浏览器开发者工具（F12）
   - 切换到"Console"标签，查看错误信息
   - 切换到"Network"标签，查看资源加载情况
   - 检查是否有 404 错误或 CORS 错误

### 7.2 测试 2：测试前端路由

1. **测试导航**：
   - 在首页，点击导航链接
   - 检查 URL 是否变化
   - 检查页面是否正常切换

2. **测试刷新**：
   - 访问某个路由页面（例如 `/login`）
   - 在地址栏直接输入完整 URL：
     ```
     http://你的bucket名称.oss-cn-地域.aliyuncs.com/login
     ```
   - 按回车访问
   - **预期结果**：
     - ✅ 不会出现 404 错误
     - ✅ 页面正常显示（因为 OSS 配置了 404 页为 index.html）
     - ✅ 前端路由正常工作

3. **如果刷新出现 404**：
   - 检查 OSS 静态网站托管配置
   - 确认"默认 404 页"设置为 `index.html`
   - 等待几分钟让配置生效
   - 重新测试

### 7.3 测试 3：检查资源加载

1. **打开浏览器开发者工具**（F12）
2. **切换到"Network"标签**
3. **刷新页面**（F5 或 Ctrl+R）
4. **检查资源加载**：
   - ✅ 所有 JS 文件状态码为 200
   - ✅ 所有 CSS 文件状态码为 200
   - ✅ 所有图片资源状态码为 200
   - ✅ 没有 404 错误
   - ✅ 没有 CORS 错误

5. **检查加载速度**：
   - 查看每个资源的加载时间
   - 如果加载很慢，考虑配置 CDN（见 CDN 配置指南）

### 7.4 测试 4：测试 API 请求（如果后端已配置）

1. **打开浏览器开发者工具**（F12）
2. **切换到"Network"标签**
3. **在前端页面执行操作**（例如登录）
4. **检查 API 请求**：
   - ✅ 请求 URL 正确（指向后端地址）
   - ✅ 响应状态码为 200 或 201
   - ✅ 响应数据正常
   - ✅ 没有 CORS 错误

5. **如果出现 CORS 错误**：
   - 检查后端环境变量 `FRONTEND_URL` 是否正确设置
   - 确认前端域名（包括协议）与后端配置一致
   - 重新部署后端服务

---

## 常见配置问题

### 问题 1：页面空白

**症状**：
- 访问 OSS 地址，页面是空白的
- 浏览器控制台有错误

**可能原因**：
1. 文件路径错误
2. `index.html` 中的资源路径不正确
3. 文件没有正确上传
4. 浏览器缓存问题

**解决方案**：

1. **检查文件结构**：
   ```bash
   # 使用 ossutil 列出文件
   ossutil ls oss://你的bucket名称/
   
   # 应该看到：
   # index.html
   # assets/
   ```

2. **检查 index.html 内容**：
   ```bash
   # 使用 ossutil 查看 index.html
   ossutil cat oss://你的bucket名称/index.html
   
   # 检查资源路径，应该是：
   # <script src="/assets/index-xxxxx.js"></script>
   # 而不是：
   # <script src="./assets/index-xxxxx.js"></script>
   ```

3. **检查浏览器控制台**：
   - 打开浏览器开发者工具（F12）
   - 查看 Console 标签的错误信息
   - 查看 Network 标签，检查哪个资源加载失败

4. **清除浏览器缓存**：
   - 按 Ctrl+Shift+R（Windows）或 Cmd+Shift+R（Mac）强制刷新
   - 或者在浏览器设置中清除缓存

### 问题 2：刷新页面出现 404

**症状**：
- 访问首页正常
- 点击导航正常
- 但刷新页面或直接输入路由 URL 出现 404

**原因**：
- OSS 静态网站托管没有配置 404 页，或配置不正确

**解决方案**：

1. **检查 OSS 配置**：
   - 进入 OSS 控制台 → 你的 Bucket → 基础设置 → 静态网站托管
   - 确认"状态"为"开启"
   - 确认"默认 404 页"设置为 `index.html`

2. **重新配置**：
   - 如果配置不正确，修改为正确的配置
   - 点击"保存"

3. **等待生效**：
   - 配置可能需要几分钟才能生效
   - 等待 5-10 分钟后重新测试

4. **清除浏览器缓存**：
   - 按 Ctrl+Shift+R 强制刷新

### 问题 3：CORS 错误

**症状**：
- 浏览器控制台出现 CORS 错误：
  ```
  Access to fetch at 'https://...' from origin 'https://...' has been blocked by CORS policy
  ```

**原因**：
- 后端 CORS 配置不正确
- 前端域名不在后端允许列表中

**解决方案**：

1. **检查后端环境变量**：
   - 在 Railway（或你的后端部署平台）检查 `FRONTEND_URL` 环境变量
   - 确认前端域名（包括协议）正确

2. **检查后端代码**：
   - 确认后端 CORS 配置支持多域名（逗号分隔）
   - 确认代码已更新（见 `backend/src/main.ts`）

3. **重新部署后端**：
   - 修改环境变量后，需要重新部署后端服务
   - 等待部署完成

4. **测试**：
   - 清除浏览器缓存
   - 重新访问前端
   - 检查是否还有 CORS 错误

### 问题 4：文件上传失败

**症状**：
- 使用 ossutil 上传文件时出错
- 错误信息：`AccessDenied` 或 `InvalidAccessKeyId`

**原因**：
- AccessKey 配置不正确
- AccessKey 权限不足

**解决方案**：

1. **检查 AccessKey**：
   - 确认 AccessKey ID 和 Secret 正确
   - 确认没有多余的空格或换行

2. **重新配置 ossutil**：
   ```bash
   ossutil config
   ```
   - 重新输入正确的 AccessKey

3. **检查 AccessKey 权限**：
   - 进入 RAM 控制台
   - 检查 AccessKey 对应的用户权限
   - 确认有 OSS 的读写权限

### 问题 5：访问速度慢

**症状**：
- 页面加载很慢
- 资源加载时间长

**原因**：
- OSS 直接访问没有 CDN 加速
- 地域选择不合适

**解决方案**：

1. **检查地域**：
   - 确认 OSS 地域选择合理（离用户近）
   - 如果地域不合适，需要重新创建 Bucket（地域无法修改）

2. **配置 CDN**：
   - 参考 CDN 配置指南
   - CDN 可以大幅提升访问速度

3. **优化文件大小**：
   - 检查构建产物大小
   - 使用代码分割和压缩
   - 优化图片资源

---

## 📋 配置检查清单

完成所有配置后，请确认：

- [ ] OSS Bucket 已创建
- [ ] Bucket 名称记录（全局唯一）
- [ ] 地域记录（无法修改）
- [ ] 存储类型：标准存储
- [ ] 读写权限：公共读
- [ ] 静态网站托管已启用
- [ ] 默认首页：index.html
- [ ] 默认 404 页：index.html ⚠️ 重要
- [ ] CORS 规则已配置
- [ ] 前端文件已上传到 OSS
- [ ] 文件结构正确（index.html 在根目录）
- [ ] OSS 直接访问地址可以正常访问
- [ ] 前端路由正常工作（刷新不 404）
- [ ] 所有资源正常加载
- [ ] 浏览器控制台没有错误

---

## 🎯 下一步

配置完成后：

1. **记录配置信息**：
   ```
   Bucket 名称：____________________
   地域：____________________
   OSS 访问地址：http://你的bucket名称.oss-cn-地域.aliyuncs.com
   ```

2. **配置后端 CORS**：
   - 在 Railway 添加环境变量：
     ```
     FRONTEND_URL=http://你的bucket名称.oss-cn-地域.aliyuncs.com
     ```
   - 重新部署后端

3. **配置 CDN**（可选，推荐）：
   - 参考 `OSS_CDN_SETUP_GUIDE.md` 的 CDN 配置部分
   - CDN 可以提供 HTTPS、更快的访问速度

4. **测试完整功能**：
   - 测试前端页面
   - 测试 API 请求
   - 测试所有功能

---

## 📞 需要帮助？

如果遇到问题：

1. **查看错误信息**：
   - 浏览器控制台（F12）
   - OSS 控制台的日志

2. **检查配置**：
   - 对照本指南检查每个配置项
   - 确认没有遗漏的步骤

3. **参考文档**：
   - 阿里云 OSS 官方文档：https://help.aliyun.com/product/31815.html
   - 快速上手指南：`OSS_CDN_QUICK_START.md`
   - CDN 配置指南：`OSS_CDN_SETUP_GUIDE.md`

---

**配置完成后，告诉我前端访问地址，我可以帮你测试！** 🚀

