# 快速部署指南（中国大陆用户）

## 🚀 最快方案：阿里云 OSS + CDN

### 第一步：购买域名（约10分钟）

1. **访问阿里云域名购买页面**
   - 网址：https://wanwang.aliyun.com
   - 注册/登录阿里云账号

2. **搜索并购买域名**
   - 输入你想要的域名（例如：`xiaohongshu-content.com`）
   - 选择 .com 后缀（约 69元/年）
   - 加入购物车并支付

3. **完成域名实名认证**（必需）
   - 进入 **"域名"** > **"域名列表"**
   - 找到购买的域名，点击 **"实名认证"**
   - 填写个人信息，上传身份证
   - 等待审核（1-3个工作日）

### 第二步：开通 OSS 服务（约5分钟）

1. **登录阿里云控制台**
   - 网址：https://ecs.console.aliyun.com

2. **搜索并开通 OSS**
   - 搜索 **"对象存储 OSS"**
   - 点击 **"开通"**，选择按量付费

3. **创建 Bucket（存储桶）**
   - 点击 **"创建 Bucket"**
   - Bucket 名称：例如 `xiaohongshu-frontend`
   - 地域：选择 **华东1-杭州**（或离你最近的）
   - 读写权限：**公共读**（重要！）
   - 其他保持默认

4. **开启静态网站托管**
   - 进入创建的 Bucket
   - 点击左侧 **"Bucket配置"** > **"静态网站托管"**
   - 点击 **"设置"** > **"开启"**
   - 设置：
     - 默认首页：`index.html`
     - 默认404页：`index.html`
   - 点击 **"保存"**

### 第三步：配置 CDN（约10分钟）

1. **开通 CDN 服务**
   - 在阿里云控制台搜索 **"CDN"**
   - 点击 **"开通CDN服务"**（首次使用需要开通，免费）

2. **添加加速域名**
   - 点击 **"添加域名"**
   - 填写信息：
     - **加速域名**：填写你购买的域名（例如：`www.xiaohongshu-content.com`）
     - **加速区域**：仅中国内地
     - **源站类型**：OSS域名
     - **源站信息**：选择你的 OSS Bucket
     - **端口**：80
   - 点击 **"提交"**

3. **等待审核**（约10-30分钟）
   - CDN 会自动审核并配置
   - 审核通过后会提供 CNAME 地址

### 第四步：绑定域名和配置 SSL（约10分钟）

1. **配置 HTTPS（免费证书）**
   - 进入 CDN 控制台 > **"域名管理"**
   - 找到你的加速域名，点击 **"管理"**
   - 点击 **"HTTPS配置"**
   - 开启 HTTPS，选择 **"免费证书"**
   - 点击 **"提交"**，等待证书签发（约10分钟）

2. **配置域名解析**
   - 进入 **"域名"** > **"解析设置"**
   - 找到你购买的域名，点击 **"解析"**
   - 添加记录：
     - **记录类型**：CNAME
     - **主机记录**：www（或其他子域名）
     - **记录值**：CDN提供的CNAME地址（如：`xiaohongshu-content.com.w.alikunlun.com`）
     - **TTL**：10分钟
   - 点击 **"确认"**

### 第五步：部署前端代码（约5分钟）

#### 方式 1：使用脚本自动部署（推荐）

**Windows 用户：**
```bash
# 1. 安装阿里云 CLI
npm install -g @alicloud/cli

# 2. 配置 AccessKey
aliyun configure
# 输入 AccessKey ID 和 AccessKey Secret（在阿里云控制台获取）

# 3. 运行部署脚本
.\scripts\deploy-aliyun-oss.bat xiaohongshu-frontend oss-cn-hangzhou
```

**Mac/Linux 用户：**
```bash
# 1. 安装阿里云 CLI
npm install -g @alicloud/cli

# 2. 配置 AccessKey
aliyun configure

# 3. 运行部署脚本
chmod +x scripts/deploy-aliyun-oss.sh
./scripts/deploy-aliyun-oss.sh xiaohongshu-frontend oss-cn-hangzhou
```

#### 方式 2：手动上传（简单）

1. **构建前端**
   ```bash
   cd frontend
   npm install
   npm run build
   ```

2. **上传文件**
   - 进入阿里云 OSS 控制台
   - 进入你的 Bucket
   - 点击 **"文件管理"** > **"上传文件"**
   - 选择 `frontend/dist` 目录中的所有文件
   - 点击 **"上传"**

### 第六步：测试访问（约5分钟）

1. **等待域名解析生效**（约10-30分钟）
   - 可以在命令行测试：`ping www.xiaohongshu-content.com`
   - 如果返回 IP 地址，说明解析已生效

2. **访问网站**
   - 打开浏览器，访问：`https://www.xiaohongshu-content.com`
   - 测试各个路由是否正常

### 第七步：域名备案（必需，约15-20个工作日）

⚠️ **注意**：使用国内服务器或CDN，域名必须备案！

1. **进入备案系统**
   - 在阿里云控制台搜索 **"ICP备案"**
   - 点击 **"开始备案"**

2. **填写备案信息**
   - 主体信息：填写个人信息或企业信息
   - 网站信息：填写网站名称、域名等
   - 上传证件照片

3. **等待审核**
   - 阿里云初审：1-2个工作日
   - 管局审核：7-20个工作日（不同省份时间不同）

4. **备案成功**
   - 收到备案成功通知
   - 备案号会以邮件形式发送

---

## 💰 费用估算

| 项目 | 费用 | 说明 |
|------|------|------|
| **域名** | 69元/年 | .com 域名 |
| **OSS存储** | 5-10元/月 | 约 10GB 存储 |
| **CDN流量** | 0.24元/GB | 按流量计费 |
| **总计（首年）** | 约 200-300元 | 包含域名费用 |
| **总计（次年）** | 约 100-200元/年 | 不含域名费用 |

**流量估算**：
- 每月 100GB 流量：约 24元
- 每月 500GB 流量：约 120元
- 每月 1TB 流量：约 240元

---

## ⚠️ 重要提醒

1. **域名备案是必需的**
   - 使用国内CDN或服务器，域名必须备案
   - 备案期间网站需要可以访问（显示备案中页面）
   - 备案时间：约15-20个工作日

2. **实名认证是必需的**
   - 国内域名（.cn, .com.cn）必须实名认证
   - 国际域名（.com）使用国内服务也需要实名认证

3. **保持网站可访问**
   - 备案期间网站必须可以访问
   - 建议显示"备案中"页面

4. **定期续费**
   - 域名需要每年续费
   - 建议设置自动续费

---

## 📞 需要帮助？

1. **阿里云客服**
   - 电话：95187
   - 在线客服：控制台右侧"云小蜜"

2. **官方文档**
   - OSS文档：https://help.aliyun.com/product/31815.html
   - CDN文档：https://help.aliyun.com/product/27099.html
   - 备案指南：https://help.aliyun.com/product/35486.html

3. **项目文档**
   - 详细部署指南：`DEPLOY_CN.md`
   - 测试部署指南：`TEST_AND_DEPLOY.md`

---

## ✅ 部署检查清单

- [ ] 已购买域名
- [ ] 域名已完成实名认证
- [ ] 已开通 OSS 服务
- [ ] 已创建 Bucket 并开启静态网站托管
- [ ] 已开通 CDN 服务
- [ ] 已添加加速域名
- [ ] 已配置 HTTPS 证书
- [ ] 已配置域名解析
- [ ] 已上传前端文件到 OSS
- [ ] 已测试网站访问
- [ ] 已提交域名备案（正在审核中）

---

**祝部署顺利！** 🎉


