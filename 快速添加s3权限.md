# ⚡ 快速添加 s3:ListBucket 权限（30秒版）

## 🎯 最简单的方法

### 步骤 1：打开 IAM 控制台

**直接访问**：https://console.aws.amazon.com/iam/

或者：
1. 访问：https://console.aws.amazon.com/
2. 在顶部搜索框输入：`IAM`
3. 点击搜索结果中的 "IAM"

### 步骤 2：找到你的用户

1. 在左侧菜单，点击 **"用户"**（Users）
2. 找到你的用户（例如：`xiaogao-uploader`）
3. 点击用户名

### 步骤 3：添加权限

1. 点击 **"权限"**（Permissions）标签
2. 点击 **"添加权限"**（Add permissions）按钮
3. 选择 **"直接附加现有策略"**（Attach policies directly）
4. 在搜索框输入：`S3Full`
5. 勾选 **`AmazonS3FullAccess`**
6. 点击 **"下一步"** → **"添加权限"**

### 步骤 4：完成

✅ 权限已添加！等待 1-2 分钟让权限生效。

---

## 📝 说明

- `AmazonS3FullAccess` 包含所有 S3 权限，包括 `s3:ListBucket`
- 这是最简单的方法，适合大多数场景
- 如果你需要最小权限，可以参考 `如何找到IAM控制台并添加权限.md` 中的方法 B

---

## ⚠️ 如果已添加 AmazonS3FullAccess 但仍然失败

**请查看**：`已添加AmazonS3FullAccess但仍失败排查.md`

**常见原因**：
1. **存储桶策略（Bucket Policy）中有 Deny 规则**（最常见！）
2. 区域不匹配
3. 存储桶名称不匹配
4. 访问密钥不属于正确的 IAM 用户

---

## 🔍 如果找不到 IAM 控制台？

1. **直接访问链接**：https://console.aws.amazon.com/iam/
2. **通过搜索**：在 AWS 控制台顶部搜索框输入 `IAM`
3. **通过菜单**：点击左上角 **"☰"** → **"安全、身份与合规"** → **"IAM"**

