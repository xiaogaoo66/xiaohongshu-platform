# 前端部署指南 - 阿里云 OSS + CDN

## ✅ 后端域名确认

你的后端域名是：**`xiaohongshu-platform-production.up.railway.app`**

---

## 📋 部署前准备

### 1. 确认文件已更新

- ✅ `frontend/env.production` - 已配置后端地址
- ✅ 构建脚本：`npm run build` - 会读取 `env.production` 文件

### 2. 准备阿里云资源

确保你已经准备好：
- ✅ 阿里云账号
- ✅ OSS 存储桶（Bucket）
- ✅ CDN 加速域名（可选，但推荐）

---

## 🚀 部署步骤

### 步骤 1：本地构建前端项目

在项目根目录执行：

```bash
# 进入前端目录
cd frontend

# 安装依赖（如果还没安装）
npm install

# 构建生产版本（会自动读取 env.production）
npm run build
```

**构建完成后**，会在 `frontend/dist` 目录生成静态文件。

**重要说明：**
- Vite 会在构建时读取 `env.production` 文件中的环境变量
- 环境变量会被注入到构建后的 JavaScript 代码中
- 确保 `env.production` 中的 `VITE_API_BASE_URL` 配置正确

---

### 步骤 2：创建 OSS 存储桶

1. 登录阿里云控制台：https://oss.console.aliyun.com/
2. 点击 **"创建 Bucket"**
3. 配置参数：
   - **Bucket 名称**：例如 `xiaohongshu-frontend`（全局唯一）
   - **地域**：选择离用户最近的地域（如：华东1-杭州）
   - **存储类型**：标准存储
   - **读写权限**：公共读（因为前端需要公开访问）
   - **版本控制**：关闭（可选）
   - **服务器端加密**：关闭（可选）
4. 点击 **"确定"** 创建

---

### 步骤 3：上传静态文件到 OSS

#### 方法 1：使用阿里云控制台上传（推荐新手）

1. 进入 OSS 控制台，选择你的 Bucket：`xhs-content`（西南成都）
2. 点击 **"文件管理"** → **"上传文件"**

3. **配置上传参数**：

   **① 上传到（上传到）**
   - ✅ 选择 **"当前目录"**（默认已选中）
   - 这样文件会直接上传到 Bucket 根目录，路径显示为 `oss://xhs-content/`
   - ❌ 不要选择"指定目录"，因为静态网站需要文件在根目录

   **② 文件 ACL（文件访问权限）**
   - ✅ **必须选择 "公共读"**（Public Read）
   - 这样访问者才能正常访问你的静态网站
   - ❌ 不要选择"继承 Bucket"，因为可能无法访问
   - ❌ 不要选择"私有"，这样网站无法公开访问
   - ❌ 不要选择"公共读写"，存在安全风险

   **③ 域名（域名）**
   - ✅ 选择 **"外网域名"**（默认已选中）
   - 外网域名：`xhs-content.oss-cn-chengdu.aliyuncs.com`
   - 这个域名用于访问你的网站

   **④ 待上传文件（待上传文件）**
   - 打开本地 `frontend/dist` 文件夹
   - **重要**：只选择 `dist` 文件夹**内部**的文件和文件夹
     - ✅ 选择 `index.html`
     - ✅ 选择 `assets/` 文件夹（包括里面的所有文件）
     - ❌ **不要选择 `dist` 文件夹本身**
   - 将选中的文件和文件夹**拖拽**到上传区域（带虚线框的区域）
   - 或者点击 **"扫描文件夹"** 按钮，选择 `dist` 文件夹

   **⑤ 高级选项（高级选项）**（可选，默认即可）
   - **存储类型**：选择 **"继承 Bucket"**（默认即可，标准存储）
   - **服务端加密方式**：选择 **"继承 Bucket"**（默认即可）
   - **用户自定义元数据**：不需要配置

4. **开始上传**：
   - 文件拖拽后，会在下方的表格中显示待上传文件列表
   - 检查文件列表，确认包含 `index.html` 和 `assets/` 文件夹
   - 点击 **"上传"** 按钮，等待上传完成

**重要注意事项：**
- ✅ 确保 `index.html` 在 Bucket 根目录（不是 `dist/index.html`）
- ✅ 文件 ACL 必须设置为 **"公共读"**，否则网站无法访问
- ✅ 上传后检查文件结构，应该是：`oss://xhs-content/index.html` 和 `oss://xhs-content/assets/...`

#### 方法 2：使用阿里云 CLI 工具（推荐）

1. **安装阿里云 CLI**：
   ```bash
   # Windows (使用 PowerShell)
   # 下载安装包：https://help.aliyun.com/document_detail/121258.html
   # 或使用 pip
   pip install aliyuncli
   ```

2. **配置访问密钥**：
   ```bash
   aliyuncli configure
   # 输入 AccessKey ID
   # 输入 AccessKey Secret
   # 输入默认地域（如：cn-hangzhou）
   ```

3. **上传文件**：
   ```bash
   # 在项目根目录执行
   cd frontend
   aliyuncli oss cp dist/ oss://你的bucket名称/ --recursive
   ```

#### 方法 3：使用 OSS Browser（图形化工具）

1. 下载 OSS Browser：https://help.aliyun.com/document_detail/61872.html
2. 登录阿里云账号
3. 选择你的 Bucket
4. 将 `frontend/dist` 目录下的所有文件拖拽上传

---

### 步骤 4：配置 OSS 静态网站托管

静态网站托管功能可以让你的 OSS Bucket 像普通网站一样访问，并且支持前端路由。

#### 操作步骤

1. **进入静态网站托管设置**
   - 在 OSS 控制台，选择你的 Bucket：`xhs-content`
   - 点击左侧菜单 **"基础设置"** → **"静态网站托管"**
   - 如果还没启用，会看到 **"设置"** 或 **"开启"** 按钮

2. **启用静态网站托管**
   - 点击 **"设置"** 或 **"开启"** 按钮
   - 会打开配置弹窗或页面

3. **配置参数**

   **① 静态网站托管状态**
   - 点击 **"开启"** 或 **"启用"** 开关
   - 状态应该变为 **"已开启"**

   **② 默认首页（默认首页文档）**
   - 输入：`index.html`
   - 这是访问网站根目录时显示的页面
   - 例如：访问 `http://xhs-content.oss-cn-chengdu.aliyuncs.com/` 会显示 `index.html`

   **③ 默认 404 页（默认 404 文档）**
   - 输入：`index.html`（**重要！必须和默认首页一样**）
   - 这是访问不存在的路径时显示的页面
   - 例如：访问 `http://xhs-content.oss-cn-chengdu.aliyuncs.com/about` 会返回 `index.html`，由前端路由处理

   **④ 子目录首页（子目录首页文档）**（可选）
   - 可以留空，或设置为 `index.html`
   - 用于子目录下的默认页面

4. **保存配置**
   - 点击 **"保存"** 或 **"确定"** 按钮
   - 等待配置生效（通常几秒钟）

5. **验证配置**
   - 配置成功后，页面会显示：
     - **静态网站托管状态**：已开启
     - **默认首页**：`index.html`
     - **默认 404 页**：`index.html`
     - **静态网站访问地址**：`http://xhs-content.oss-cn-chengdu.aliyuncs.com`（或类似的地址）

#### 为什么默认 404 页要设置为 index.html？

对于使用 Vue Router 或 React Router 的单页应用（SPA）：

- **前端路由**：所有路由都在前端处理，如 `/home`、`/about`、`/login` 等
- **服务器配置**：当用户直接访问 `/about` 时，OSS 找不到这个文件，会返回 404
- **解决方案**：将默认 404 页设置为 `index.html`，这样：
  - 访问 `/about` → OSS 返回 `index.html` → 前端路由识别 `/about` → 显示正确的页面
  - 用户刷新页面不会出现 404 错误

**重要提醒：**
- ✅ 默认 404 页必须设置为 `index.html`，否则刷新页面会出错
- ✅ 确保 `index.html` 文件已经上传到 Bucket 根目录
- ✅ 如果修改了配置，可能需要等待几秒钟才能生效

---

### 步骤 5：获取 OSS 访问地址并测试

配置完静态网站托管后，你需要获取访问地址并测试网站是否正常。

#### 获取访问地址

1. **在静态网站托管页面查看**
   - 配置保存后，在静态网站托管页面会显示：
     - **静态网站访问地址**：`http://xhs-content.oss-cn-chengdu.aliyuncs.com`
     - 或 **Bucket 外网访问地址**：`http://xhs-content.oss-cn-chengdu.aliyuncs.com`
   - 这两个地址通常是一样的

2. **或者通过 Bucket 概览查看**
   - 在 OSS 控制台，选择 Bucket `xhs-content`
   - 在 **"概览"** 页面，找到 **"访问域名"** 部分
   - 复制 **"外网访问"** 的地址

3. **记录访问地址**
   - 你的网站地址应该是：`http://xhs-content.oss-cn-chengdu.aliyuncs.com`
   - 保存这个地址，用于后续访问和测试

#### 测试访问

1. **在浏览器中打开**
   - 复制访问地址，在浏览器中打开
   - 应该能看到你的前端页面

2. **测试前端路由**
   - 在网站中点击导航链接，跳转到其他页面（如 `/about`、`/login` 等）
   - 在浏览器地址栏中，直接访问一个路由（如 `http://xhs-content.oss-cn-chengdu.aliyuncs.com/login`）
   - 按 **F5 刷新页面**，应该不会出现 404 错误
   - ✅ 如果刷新后正常显示页面，说明静态网站托管配置成功

3. **检查浏览器控制台**
   - 按 **F12** 打开开发者工具
   - 切换到 **"Console"** 标签
   - 查看是否有错误信息
   - 切换到 **"Network"** 标签
   - 刷新页面，检查资源是否正常加载（JS、CSS、图片等）

4. **测试 API 连接**
   - 尝试登录功能或其他需要调用后端 API 的操作
   - 在 **"Network"** 标签中查看 API 请求
   - 确认 API 请求地址是：`https://xiaohongshu-platform-production.up.railway.app/...`
   - ✅ 如果 API 请求成功，说明前后端连接正常

#### 常见问题排查

**问题 1：页面显示空白**
- 检查浏览器控制台的错误信息
- 确认 `index.html` 和 `assets/` 文件夹都已正确上传
- 检查文件 ACL 是否设置为"公共读"

**问题 2：刷新页面出现 404**
- 检查静态网站托管的默认 404 页是否设置为 `index.html`
- 确认 `index.html` 文件在 Bucket 根目录

**问题 3：API 请求失败（CORS 错误）**
- 需要检查后端 CORS 配置，确保允许 OSS 域名访问
- 前端域名：`xhs-content.oss-cn-chengdu.aliyuncs.com`

**问题 4：访问地址直接下载文件（而不是显示网页）** ⚠️ 重要！

这是最常见的问题！当你点击文件详情页的 URL（如 `https://xhs-content.oss-cn-chengdu.aliyuncs.com/index.html`）时，浏览器会下载文件而不是显示网页。

**原因：**
- OSS 控制台文件详情页显示的 URL 是 **OSS 默认域名**，用于文件下载，不是用于网页浏览
- 即使文件的 Content-Type 是 `text/html`，使用默认域名也会触发下载（这是 OSS 的默认行为）
- 阿里云会在文件详情页显示黄色警告提醒这一点

**解决方案（必须启用静态网站托管）：**

1. **启用静态网站托管**（最重要！）
   - 进入 OSS 控制台 → 选择 Bucket `xhs-content`
   - 点击左侧菜单 **"基础设置"** → **"静态网站托管"**
   - 如果状态显示 **"未开启"** 或 **"关闭"**，请：
     - 点击 **"设置"** 或 **"编辑"** 按钮
     - **开启**静态网站托管状态
     - 设置默认首页：`index.html`
     - 设置默认 404 页：`index.html`
     - 点击 **"保存"**
     - **等待 1-2 分钟**让配置生效

2. **获取静态网站访问地址**
   - 在静态网站托管页面，找到 **"静态网站访问地址"** 或 **"访问地址"**
   - 这个地址通常格式是：`http://xhs-content.oss-cn-chengdu.aliyuncs.com`
   - ⚠️ **重要**：这个地址和文件详情页的 URL 虽然格式相同，但功能不同！
   - ✅ **使用静态网站访问地址**：会渲染网页
   - ❌ **使用文件详情页的 URL**：会下载文件

3. **验证访问**
   - 复制静态网站托管页面显示的访问地址
   - 在浏览器中打开（直接访问根目录，不要加 `/index.html`）
   - 例如：`http://xhs-content.oss-cn-chengdu.aliyuncs.com`（不要加 `/index.html`）
   - 应该能看到网页内容，而不是下载文件

4. **如果还是下载文件**
   - 确认使用的是静态网站托管页面显示的地址（不是文件详情页的 URL）
   - 确保访问的是根目录（不带文件名），让 OSS 自动返回 `index.html`
   - 清除浏览器缓存（Ctrl + Shift + Delete）
   - 使用无痕模式访问（Ctrl + Shift + N）
   - 等待 2-3 分钟让配置完全生效

**关键区别：**
- ❌ **文件详情页 URL**：`https://xhs-content.oss-cn-chengdu.aliyuncs.com/index.html` → 会下载
- ✅ **静态网站地址**：`http://xhs-content.oss-cn-chengdu.aliyuncs.com` → 会渲染网页
- ✅ **静态网站地址（带路径）**：`http://xhs-content.oss-cn-chengdu.aliyuncs.com/` → 会渲染网页

**注意：**
- 文件的 Content-Type 已经是 `text/html`，这是正确的，不需要修改
- 文件 ACL 已经是 `公共读`，这也是正确的
- 问题在于必须使用静态网站托管功能，而不是直接访问文件 URL

---

### 步骤 6：绑定自定义域名到 OSS 静态网站托管（推荐）

如果你购买了自定义域名（如 `www.yourdomain.com` 或 `frontend.yourdomain.com`），可以将其绑定到 OSS 静态网站托管，这样访问更专业，也便于管理。

#### 前置要求

1. **已购买域名**（在阿里云或其他域名服务商）
2. **域名已备案**（如果使用国内 OSS，域名必须备案；如果使用海外 OSS，可能不需要备案）
3. **已启用静态网站托管**（步骤 4 已完成）

---

#### ⚠️ 重要：域名备案流程

**如果你的域名还没有备案**（如你遇到 "域名已备案" 显示红色 X 的情况），需要先完成备案。以下是详细流程：

##### 什么是域名备案？

- **ICP 备案**（Internet Content Provider，互联网内容提供商备案）是中国大陆的法定要求
- 使用国内云服务（如阿里云 OSS 国内节点）时，域名必须完成备案
- 备案是**免费的**，但需要**审核时间**（通常 7-20 个工作日）

##### 备案需要准备的材料

1. **域名**（已购买，如 `xhscontent.cn`）
2. **服务器/云服务**（你的阿里云 OSS Bucket）
3. **个人备案**：
   - 身份证正反面照片
   - 手机号码（需要接收验证码）
   - 邮箱地址
4. **企业备案**：
   - 营业执照照片
   - 法人身份证正反面
   - 企业公章（部分地区需要）
   - 负责人身份证和手机号

##### 备案流程（阿里云）

**方式一：在阿里云备案系统备案（推荐）**

1. **进入备案系统**
   - 访问：https://beian.aliyun.com/
   - 或登录阿里云控制台 → 产品与服务 → 备案服务

2. **开始备案**
   - 点击 **"开始备案"** 按钮
   - 选择备案类型：
     - **首次备案**：域名和服务器都是第一次备案
     - **新增网站**：域名已备案过，新增网站
     - **变更备案**：修改已有备案信息

3. **填写备案信息**
   
   **① 验证备案类型**
   - 填写你的域名（如 `xhscontent.cn`）
   
   **⚠️ 重要：关于服务器/服务码的要求**
   
   在备案过程中，系统可能会提示 **"账号下可用的服务器/服务码不足"**，这是因为：
   - 阿里云备案系统要求必须有**服务器**或**服务码**才能备案
   - 虽然 OSS 可以用于备案，但系统可能仍然要求提供服务器/服务码
   - 这是备案系统的技术限制，不是你的操作问题
   
   **⚠️ 最新政策变化：按量付费 ECS 不再支持备案**
   
   - 阿里云已更新政策，**按量付费的 ECS 不再可以用于备案**
   - 必须使用**包年包月的 ECS** 或**轻量应用服务器**才能备案
   - 或者使用**备案服务码**（从朋友的服务器获取）
   
   **解决方案（五选一）：**
   
   **方案一：购买包年包月的 ECS 服务器（推荐）**
   - 购买一台最低配置的包年包月 ECS 服务器用于备案
   - **价格**：约 100-200 元/年（最低配置，1核1G）
   - **配置建议**：选择最低配置即可（1核1G，仅用于备案）
   - 购买后，在备案时选择 **"ECS 云服务器"** 而不是 OSS
   - 备案完成后，可以继续使用 OSS 托管前端，服务器可以保留备用或释放
   
   **购买包年包月 ECS 步骤：**
   1. 登录阿里云控制台：https://ecs.console.aliyun.com/
   2. 点击 **"创建实例"** 或 **"购买实例"**
   3. 选择配置：
      - **付费方式**：**包年包月**（必须选择，按量付费不支持备案）
      - **购买时长**：选择 **1年**（通常有优惠，约 100-200 元）
      - **地域**：选择与 OSS 相同的地域（如成都）
      - **实例规格**：选择最便宜的（如 `ecs.t6-c1m1.large` 或 `ecs.t5-lc1m1.small`）
      - **操作系统**：选择 CentOS 或 Ubuntu（任意）
      - **存储**：选择 20GB 系统盘即可
   4. 点击 **"确认订单"** 并支付
   5. 购买成功后，在备案时选择这台 ECS 服务器
   
   **方案二：购买轻量应用服务器（更便宜，推荐）**
   - 轻量应用服务器通常比 ECS 更便宜，且支持备案
   - **价格**：约 60-120 元/年（最低配置）
   - **优势**：价格更低，配置足够备案使用
   - **购买位置**：阿里云控制台 → 轻量应用服务器 → 购买
   - **配置**：选择最低配置（1核1G，20GB 系统盘）
   
   **购买轻量应用服务器步骤：**
   1. 登录阿里云控制台：https://swas.console.aliyun.com/
   2. 点击 **"购买轻量应用服务器"**
   3. 选择配置：
      - **购买时长**：选择 **1年**
      - **地域**：选择与 OSS 相同的地域
      - **镜像**：选择 CentOS 或 Ubuntu
      - **配置**：选择最低配置（约 60-120 元/年）
   4. 点击 **"立即购买"** 并支付
   5. 购买成功后，在备案时选择这台轻量应用服务器
   
   **方案三：使用备案服务码（最省钱，推荐）**
   - 如果你有朋友或同事有阿里云服务器（包年包月或轻量），可以让他们生成**备案服务码**给你使用
   - 每个 ECS 实例或轻量应用服务器可以生成 5 个备案服务码
   - 生成位置：
     - ECS：ECS 控制台 → 实例详情 → 备案服务码
     - 轻量应用服务器：轻量应用服务器控制台 → 服务器详情 → 备案服务码
   - 使用服务码备案，**完全免费**，不需要购买服务器
   - **这是最省钱的方案！**
   
   **方案四：使用其他云服务商（如腾讯云、华为云）**
   - 如果阿里云政策太严格，可以考虑其他云服务商
   - **腾讯云**：轻量应用服务器约 60-100 元/年，支持备案
   - **华为云**：弹性云服务器约 100-150 元/年，支持备案
   - 其他云服务商的备案政策可能更宽松
   
   **方案五：使用海外服务器（无需备案）**
   - 如果不想备案，可以使用**香港/海外服务器**
   - **优势**：无需备案，即买即用
   - **劣势**：访问速度可能稍慢，价格稍高
   - **推荐**：阿里云香港轻量应用服务器（约 24-60 元/月）
   - 注意：使用海外服务器，域名不需要备案，但访问速度可能不如国内快
   
   **选择服务器类型：**
   - 如果购买了 ECS：选择 **"ECS 云服务器"**，然后选择你的实例
   - 如果有服务码：选择 **"使用服务码备案"**，输入服务码
   - 如果系统允许：选择 **"其他阿里云产品"** → **"对象存储 OSS"**（但可能不成功）

   **② 填写主体信息**
   - **个人备案**：
     - 姓名、身份证号
     - 身份证正反面照片上传
     - 手机号、邮箱
     - 通信地址（详细地址）
   - **企业备案**：
     - 企业名称、统一社会信用代码
     - 营业执照照片
     - 法人信息、负责人信息

   **③ 填写网站信息**
   - **网站名称**：例如 "视频内容平台" 或 "XHS内容平台"（不能太商业化）
   - **网站内容**：选择 **"其他"** 或 **"个人网站"**
   - **网站语言**：中文
   - **网站域名**：填写你要备案的域名（如 `xhscontent.cn`）
   - **网站服务内容**：选择 **"其他"** 或根据实际情况选择

4. **上传备案资料**
   - 根据要求上传身份证、营业执照等照片
   - 确保照片清晰、完整
   - 照片大小不超过 4MB

5. **提交审核**
   - 检查所有信息无误后，点击 **"提交审核"**
   - 系统会生成备案订单号

6. **等待初审（1-3 个工作日）**
   - 阿里云会对你的资料进行初步审核
   - 审核通过后，会收到短信/邮件通知
   - 如果资料有问题，会通知你修改

7. **拍照验证（部分地区需要）**
   - 部分地区要求现场拍照或视频验证
   - 按照通知要求完成验证

8. **提交管局审核（7-20 个工作日）**
   - 阿里云初审通过后，会提交到各省通信管理局
   - 这是最长的等待阶段
   - 可以随时在备案系统查看进度

9. **备案完成**
   - 管局审核通过后，会收到备案成功的短信/邮件
   - 备案号格式：`京ICP备12345678号` 或 `京ICP备12345678号-1`
   - **备案成功后，就可以绑定域名到 OSS 了！**

##### 备案期间可以做什么？

- ✅ **可以使用 OSS 的默认域名**（如 `xhs-content.oss-cn-chengdu.aliyuncs.com`）访问
- ✅ **可以继续开发项目**
- ❌ **不能绑定自定义域名**（必须等备案完成）

##### 备案常见问题

**Q1：备案需要多长时间？**
- 阿里云初审：1-3 个工作日
- 管局审核：7-20 个工作日（不同省份时间不同）
- 总计：约 10-25 个工作日

**Q2：备案需要收费吗？**
- 备案本身**完全免费**，不需要任何费用
- 但如果需要购买服务器用于备案，需要支付服务器费用：
  - **包年包月 ECS**：约 100-200 元/年（最低配置）
  - **轻量应用服务器**：约 60-120 元/年（最低配置，更便宜）
  - **备案服务码**：免费（如果朋友有服务器可以借给你）

**Q3：为什么备案还需要服务器/服务码？我只用 OSS 不行吗？**
- 这是阿里云备案系统的技术限制，即使使用 OSS 托管，也需要提供服务器或服务码
- **⚠️ 重要**：按量付费的 ECS 不再支持备案，必须使用包年包月的服务器
- **解决方案**：
  - **方案一**：购买包年包月 ECS（约 100-200 元/年）或轻量应用服务器（约 60-120 元/年，更便宜）
  - **方案二**：使用朋友的备案服务码（每个服务器可生成 5 个，完全免费）
  - **方案三**：使用其他云服务商（如腾讯云、华为云）
  - **方案四**：使用海外服务器（无需备案，但访问速度可能稍慢）
- **重要**：备案完成后，你仍然可以使用 OSS 托管前端，不需要服务器运行网站

**Q4：域名在海外服务商购买的，可以备案吗？**
- 可以，但需要在阿里云有服务器或云服务（如 OSS）
- 备案流程相同

**Q5：备案期间网站可以访问吗？**
- 可以使用 OSS 默认域名访问
- 不能使用自定义域名访问（等备案完成）

**Q6：备案信息填错了怎么办？**
- 可以在备案系统中修改
- 或联系阿里云客服协助

**Q7：如何查询备案进度？**
- 登录 https://beian.aliyun.com/
- 在"我的备案"中查看订单状态

##### 备案后需要做什么？

1. **在网站底部添加备案号**
   - 需要在网站首页底部显示备案号
   - 格式：`京ICP备12345678号`（链接到 https://beian.miit.gov.cn/）
   - 这是法律要求，必须添加

2. **绑定域名到 OSS**
   - 备案成功后，回到步骤 6.1 继续绑定域名

---

#### 6.1 在 OSS 控制台绑定自定义域名

**⚠️ 注意：请确保域名已完成备案，否则无法绑定！**

1. **进入域名管理**
   - 登录阿里云 OSS 控制台：https://oss.console.aliyun.com/
   - 选择你的 Bucket：`xhs-content`
   - 点击左侧菜单 **"传输管理"** → **"域名管理"**（或 **"基础设置"** → **"域名管理"**）

2. **添加绑定域名**
   - 点击 **"绑定域名"** 或 **"添加域名"** 按钮
   - 会打开绑定域名配置页面

3. **配置域名信息**
   
   **① 域名**
   - 输入你的自定义域名，例如：`frontend.yourdomain.com` 或 `www.yourdomain.com`
   - ⚠️ **注意**：
     - 不要带 `http://` 或 `https://`，只输入域名本身
     - 如果使用子域名（如 `frontend.yourdomain.com`），确保该子域名已解析到你的主域名

   **② 绑定类型**
   - 选择 **"静态网站托管"**（重要！）
   - 不要选择 **"OSS 域名"**，因为 OSS 域名会触发下载，而不是渲染网页

   **③ HTTPS 设置**（推荐）
   - 选择 **"HTTPS 访问"**
   - 可以选择：
     - **"免费证书"**（阿里云提供的免费 SSL 证书，推荐）
     - 或 **"上传证书"**（如果你有自己的 SSL 证书）

4. **保存配置**
   - 点击 **"确定"** 或 **"提交"**
   - 系统会显示 **CNAME 记录值**（类似：`xhs-content.oss-cn-chengdu.aliyuncs.com`）
   - ⚠️ **重要**：复制这个 CNAME 值，下一步需要在域名服务商配置

#### 6.2 在域名服务商配置 DNS 解析

需要在你的域名服务商（如阿里云域名、腾讯云、GoDaddy 等）添加 CNAME 解析记录。

**如果域名在阿里云：**

1. **进入域名解析控制台**
   - 登录阿里云控制台：https://dns.console.aliyun.com/
   - 或访问：https://dc.console.aliyun.com/
   - 找到你的域名，点击 **"解析设置"**

2. **添加 CNAME 记录**
   - 点击 **"添加记录"** 按钮
   - 配置参数：
     - **记录类型**：选择 **"CNAME"**
     - **主机记录**：
       - 如果绑定的是 `frontend.yourdomain.com`，填写 `frontend`
       - 如果绑定的是 `www.yourdomain.com`，填写 `www`
       - 如果绑定的是根域名 `yourdomain.com`，填写 `@` 或留空（取决于服务商）
     - **记录值**：粘贴从 OSS 控制台复制的 CNAME 值（如：`xhs-content.oss-cn-chengdu.aliyuncs.com`）
       - ⚠️ **注意**：不要带 `http://` 或 `https://`，只输入 CNAME 值本身
     - **TTL**：选择 **"10 分钟"** 或默认值
   - 点击 **"确定"** 保存

**如果域名在其他服务商（如腾讯云、GoDaddy、Namecheap 等）：**

1. 登录你的域名服务商控制台
2. 找到 **"DNS 管理"** 或 **"域名解析"** 功能
3. 添加 CNAME 记录：
   - **主机名/子域名**：`frontend`（如果域名是 `frontend.yourdomain.com`）
   - **类型**：`CNAME`
   - **指向/目标**：粘贴 OSS 控制台显示的 CNAME 值
   - **TTL**：600 或 10 分钟

#### 6.3 等待 DNS 解析生效

1. **DNS 解析生效时间**
   - 通常需要 **10 分钟到 2 小时**，最长不超过 48 小时
   - 可以在命令行测试解析是否生效：
     ```bash
     # Windows PowerShell
     nslookup frontend.yourdomain.com
     
     # 或使用 ping
     ping frontend.yourdomain.com
     ```
   - 如果返回的 IP 地址是 OSS 的 IP，说明解析已生效

2. **验证 OSS 绑定状态**
   - 回到 OSS 控制台 → 域名管理页面
   - 查看你添加的域名状态
   - 如果显示 **"已绑定"** 或 **"正常"**，说明绑定成功

#### 6.4 测试自定义域名访问

1. **等待解析生效后**（通常 10-30 分钟），在浏览器中访问：
   ```
   https://frontend.yourdomain.com
   ```
   - ⚠️ 如果配置了 HTTPS，使用 `https://`
   - 如果还没生效，使用 `http://` 测试

2. **验证功能**
   - ✅ 页面正常加载
   - ✅ 前端路由正常工作（刷新页面不会 404）
   - ✅ API 请求正常（检查浏览器 Network 标签）

3. **如果访问失败**
   - 检查 DNS 解析是否生效（使用 `nslookup` 命令）
   - 检查 OSS 域名绑定状态是否正常
   - 检查域名是否已备案（国内 OSS 需要）
   - 等待更长时间（DNS 解析可能需要 1-2 小时）

#### 6.5 配置 HTTP 强制跳转 HTTPS（推荐）

如果配置了 HTTPS，建议开启 HTTP 自动跳转到 HTTPS：

1. **在 OSS 域名管理页面**
   - 找到你绑定的域名
   - 点击 **"HTTPS 配置"** 或 **"证书管理"**
   - 开启 **"HTTP 强制跳转 HTTPS"** 选项
   - 保存配置

2. **验证跳转**
   - 访问 `http://frontend.yourdomain.com`
   - 应该自动跳转到 `https://frontend.yourdomain.com`

#### 常见问题

**问题 1：域名绑定失败**

- 检查域名是否已备案（国内 OSS 必须）
- 检查 CNAME 记录是否正确配置
- 检查域名格式是否正确（不要带协议）

**问题 2：DNS 解析不生效**

- 等待更长时间（最长 48 小时）
- 清除本地 DNS 缓存：
  ```bash
  # Windows
  ipconfig /flushdns
  ```
- 使用不同的 DNS 服务器测试（如 8.8.8.8）

**问题 3：HTTPS 证书申请失败**

- 确保域名解析已生效
- 检查域名是否已备案
- 尝试使用阿里云免费证书
- 如果还是失败，可以先用 HTTP，稍后再配置 HTTPS

**问题 4：访问自定义域名还是下载文件**

- 确保在 OSS 绑定域名时选择了 **"静态网站托管"** 类型，而不是 **"OSS 域名"**
- 检查静态网站托管是否已启用
- 清除浏览器缓存后重试

---

### 步骤 7：配置 CDN 加速（可选，更高级）

CDN 可以加速静态资源访问，提升用户体验。

#### 7.1 添加 CDN 域名

1. 登录阿里云 CDN 控制台：https://cdn.console.aliyun.com/
2. 点击 **"添加域名"**
3. 配置参数：
   - **加速域名**：例如 `frontend.yourdomain.com`（需要先备案，或使用临时测试域名）
   - **业务类型**：选择 **"全站加速"** 或 **"静态加速"**
   - **源站信息**：
     - **源站类型**：OSS 域名
     - **源站地址**：选择你的 OSS Bucket
   - **加速区域**：全部区域
4. 点击 **"下一步"**，完成配置

#### 7.2 配置 CDN 缓存策略

1. 进入 CDN 控制台，选择你的域名
2. 点击 **"缓存配置"**
3. 配置缓存规则：
   - **HTML 文件**（如 `index.html`）：
     - 缓存时间：**0 秒**（不缓存，确保更新及时生效）
   - **静态资源**（如 `.js`, `.css`, `.png` 等）：
     - 缓存时间：**1 年**（长期缓存，减少请求）
   - **其他文件**：
     - 缓存时间：**1 天**

#### 7.3 配置 HTTPS（推荐）

1. 在 CDN 控制台，选择你的域名
2. 点击 **"HTTPS 配置"**
3. 开启 HTTPS，选择：
   - **免费证书**（阿里云提供的免费证书）
   - 或上传你的 SSL 证书
4. 开启 **"HTTP 强制跳转 HTTPS"**

---

## 📝 验证部署

### 1. 测试 OSS 直接访问

访问 OSS 静态网站地址：
```
http://你的bucket名称.oss-cn-地域.aliyuncs.com
```

应该能看到前端页面正常加载。

### 2. 测试 CDN 访问（如果配置了）

访问 CDN 域名：
```
https://frontend.yourdomain.com
```

### 3. 功能测试

- ✅ 页面正常加载
- ✅ 登录功能（调用后端 API）
- ✅ 内容管理功能
- ✅ 前端路由（刷新页面不会 404）

### 4. 检查 API 请求

1. 打开浏览器开发者工具（F12）
2. 切换到 **"Network"** 标签
3. 执行登录操作
4. 查看 API 请求：
   - 请求 URL 应该是：`https://xiaohongshu-platform-production.up.railway.app/...`
   - 不应该出现 CORS 错误

---

## 🔧 常见问题

### 问题 1：页面空白

**可能原因：**
- 文件路径错误
- `index.html` 中的资源路径不正确

**解决方案：**
- 检查 `vite.config.ts` 中的 `base` 配置（如果 OSS 有子目录）
- 确保所有文件都正确上传到 OSS
- 检查浏览器控制台的错误信息

### 问题 2：刷新页面出现 404

**原因：**
- OSS 静态网站托管没有配置默认 404 页

**解决方案：**
- 在 OSS 控制台 → 静态网站托管 → 设置默认 404 页为 `index.html`

### 问题 3：API 请求失败（CORS 错误）

**原因：**
- 后端 CORS 配置不允许前端域名访问

**解决方案：**
- 检查后端 CORS 配置，添加前端域名到允许列表
- 前端域名：你的 CDN 域名或 OSS 域名

### 问题 4：环境变量未生效

**原因：**
- 构建时没有读取到 `env.production` 文件

**解决方案：**
- 确认 `env.production` 文件在 `frontend` 目录下
- 确认文件内容格式正确（每行 `KEY=VALUE`）
- 重新构建：`npm run build`

### 问题 5：CDN 缓存导致更新不及时

**解决方案：**
- HTML 文件（`index.html`）应该设置为不缓存（0 秒）
- 静态资源文件（JS/CSS）可以长期缓存，Vite 会自动添加 hash 后缀
- 如果更新后看不到新内容，可以清除 CDN 缓存

---

## 📊 部署架构

```
用户浏览器
    ↓
CDN 加速（可选）: frontend.yourdomain.com
    ↓
OSS 静态网站托管: bucket.oss-cn-xxx.aliyuncs.com
    ↓ (API 请求)
后端 API（Railway）: xiaohongshu-platform-production.up.railway.app
    ↓
数据库（Railway PostgreSQL）
```

---

## 🔄 更新部署流程

每次更新前端代码后，需要重新部署：

1. **本地构建**：
   ```bash
   cd frontend
   npm run build
   ```

2. **上传到 OSS**：
   - 删除 OSS 中的旧文件（可选，但推荐）
   - 上传新的 `dist` 目录内容

3. **清除 CDN 缓存**（如果配置了 CDN）：
   - 在 CDN 控制台 → 刷新预热 → 刷新 URL
   - 输入：`https://frontend.yourdomain.com/index.html`

---

## 🎯 自动化部署（可选）

可以配置 GitHub Actions 或 CI/CD 工具自动部署：

1. **GitHub Actions**：推送代码后自动构建并上传到 OSS
2. **阿里云云效**：配置自动化流水线
3. **自定义脚本**：编写部署脚本，一键上传

---

## 📞 需要帮助？

如果遇到问题：
1. 检查 OSS 文件是否正确上传
2. 检查静态网站托管配置
3. 检查浏览器控制台的错误信息
4. 验证后端 API 是否正常运行

---

## ✅ 检查清单

部署前确认：
- [ ] `frontend/env.production` 已配置后端地址
- [ ] 本地构建成功（`npm run build`）
- [ ] OSS Bucket 已创建，权限设置为公共读
- [ ] 静态文件已上传到 OSS
- [ ] 静态网站托管已启用，默认页设置为 `index.html`
- [ ] CDN 已配置（可选）
- [ ] 后端 CORS 已配置允许前端域名
- [ ] 功能测试通过

---

**部署完成后，告诉我前端访问地址，我可以帮你测试！** 🚀

---

## 🌐 使用 IP 访问的部署方案（ECS 服务器）

### 📌 方案说明

如果你**不想用域名**，想直接用 **IP 地址**访问网站，可以使用 **ECS 服务器部署**。

**重要区别：**
- ❌ **OSS 静态网站托管**：只能用域名访问（如 `bucket.oss-cn-xxx.aliyuncs.com`），不支持 IP
- ✅ **ECS 服务器部署**：可以用 IP 直接访问（如 `http://123.45.67.89`），无需域名和备案

### 🎯 适用场景

- ✅ 不想购买域名
- ✅ 不想备案（国内服务器需要备案，但可以用 IP 访问）
- ✅ 测试环境或内部使用
- ✅ 已经有 ECS 服务器

### 🚀 部署步骤

#### 步骤 1：购买 ECS 服务器

1. **登录阿里云控制台**：https://ecs.console.aliyun.com/
2. **购买服务器**（如果是测试，可以选择最便宜的配置）：
   - **付费方式**：包年包月（约 100-200 元/年）或轻量应用服务器（约 60-120 元/年）
   - **地域**：选择离你最近的地域
   - **实例规格**：最低配置即可（1核1G）
   - **操作系统**：CentOS 7/8 或 Ubuntu 20.04/22.04
   - **存储**：20GB 系统盘
3. **配置安全组**（重要！）：
   - 在 ECS 控制台 → 实例 → 安全组
   - 添加入站规则：**允许 HTTP（80端口）** 和 **HTTPS（443端口）**
   - 源地址：`0.0.0.0/0`（允许所有 IP 访问）

#### 步骤 2：连接服务器并安装 Nginx

1. **使用 SSH 连接服务器**：
   ```bash
   ssh root@你的服务器IP
   ```
   
2. **更新系统**：
   ```bash
   # CentOS
   yum update -y
   
   # Ubuntu
   apt update && apt upgrade -y
   ```

3. **安装 Nginx**：
   ```bash
   # CentOS
   yum install -y nginx
   
   # Ubuntu
   apt install -y nginx
   ```

4. **启动 Nginx**：
   ```bash
   systemctl start nginx
   systemctl enable nginx  # 设置开机自启
   ```

5. **测试访问**：
   - 在浏览器访问：`http://你的服务器IP`
   - 应该看到 Nginx 默认页面

#### 步骤 3：上传前端文件到服务器

**方法 1：使用 SCP 上传（推荐）**

在**本地电脑**执行（Windows 可以使用 PowerShell 或 Git Bash）：

```bash
# 进入前端目录
cd frontend

# 构建项目
npm run build

# 上传 dist 目录到服务器
scp -r dist/* root@你的服务器IP:/usr/share/nginx/html/
```

**方法 2：使用 FTP 工具**

1. 使用 FileZilla、WinSCP 等工具连接服务器
2. 上传 `frontend/dist` 目录下的所有文件到服务器的 `/usr/share/nginx/html/` 目录

**方法 3：在服务器上直接下载**

```bash
# 在服务器上执行
cd /usr/share/nginx/html
# 使用 wget 或 curl 下载你上传到其他地方的 dist 文件
# 或者使用 git clone 下载项目，然后在服务器上构建
```

#### 步骤 4：配置 Nginx

1. **备份默认配置**：
   ```bash
   cp /etc/nginx/nginx.conf /etc/nginx/nginx.conf.bak
   ```

2. **编辑 Nginx 配置**：
   ```bash
   vi /etc/nginx/nginx.conf
   ```
   
   或者使用项目中的 `nginx.conf`：
   ```bash
   # 在本地，将 frontend/nginx.conf 上传到服务器
   scp frontend/nginx.conf root@你的服务器IP:/etc/nginx/nginx.conf
   ```

3. **验证配置**：
   ```bash
   nginx -t
   ```

4. **重启 Nginx**：
   ```bash
   systemctl restart nginx
   ```

#### 步骤 5：测试访问

1. **在浏览器访问**：`http://你的服务器IP`
2. **测试功能**：
   - ✅ 页面正常加载
   - ✅ 前端路由正常工作（刷新页面不会 404）
   - ✅ API 请求正常（检查浏览器 Network 标签）

#### 步骤 6：配置 HTTPS（可选，需要域名）

如果后续想要 HTTPS，可以：
1. 购买域名并解析到服务器 IP
2. 使用 Let's Encrypt 免费证书：
   ```bash
   # 安装 Certbot
   yum install -y certbot python3-certbot-nginx  # CentOS
   apt install -y certbot python3-certbot-nginx  # Ubuntu
   
   # 申请证书
   certbot --nginx -d 你的域名.com
   ```

### 🔧 Nginx 配置说明

项目中的 `frontend/nginx.conf` 已经配置好了：
- ✅ 处理前端路由（刷新页面不会 404）
- ✅ 静态资源缓存
- ✅ 监听 80 端口

如果需要修改，编辑 `/etc/nginx/nginx.conf` 后重启 Nginx。

### ⚠️ 注意事项

1. **安全组配置**：
   - 必须开放 80 端口（HTTP）和 443 端口（HTTPS）
   - 建议限制 SSH（22端口）的访问来源

2. **防火墙配置**：
   ```bash
   # CentOS (firewalld)
   firewall-cmd --permanent --add-service=http
   firewall-cmd --permanent --add-service=https
   firewall-cmd --reload
   
   # Ubuntu (ufw)
   ufw allow 80/tcp
   ufw allow 443/tcp
   ufw enable
   ```

3. **更新部署**：
   - 每次更新前端代码后，重新构建并上传到服务器
   - 可以编写脚本自动化部署

4. **后端 CORS 配置**：
   - 在后端添加允许的源：`http://你的服务器IP`
   - 例如：`FRONTEND_URL=http://123.45.67.89`

### 💰 成本对比

| 方案 | 成本 | 访问方式 | 是否需要备案 |
|------|------|----------|--------------|
| OSS 静态网站托管 | 约 0.1-0.5 元/月（存储+流量） | 域名访问 | 需要（如果绑定自定义域名） |
| ECS 服务器部署 | 约 60-200 元/年（服务器） | IP 或域名 | 需要（如果用域名），但可以用 IP 访问 |
| 轻量应用服务器 | 约 60-120 元/年 | IP 或域名 | 需要（如果用域名），但可以用 IP 访问 |

### 🎯 推荐方案

- **如果只是测试**：使用 ECS IP 访问，无需域名和备案
- **如果要正式使用**：建议使用域名 + OSS（更便宜，更稳定）
- **如果已经有服务器**：直接用服务器部署，用 IP 访问

---

**部署完成后，告诉我你的服务器 IP，我可以帮你测试！** 🚀

---

## 🌐 Spaceship + Vercel 自定义域名配置指南

如果你已经购买了域名（例如：`acgmbti.online`），并且域名在 Spaceship 管理，使用 Spaceship 默认名称服务器，可以按照以下步骤配置 Vercel 自定义域名。

---

### 📝 第一步：在 Vercel 添加域名并获取 DNS 配置信息

#### 1.1 登录 Vercel 控制台

1. 访问：https://vercel.com
2. 登录你的账号（如果没有账号，需要先注册）

#### 1.2 进入项目设置

1. 在 Vercel 控制台，找到你的前端项目
2. 点击项目名称进入项目详情
3. 点击顶部菜单 **"Settings"**（设置）
4. 在左侧菜单中，点击 **"Domains"**（域名）

#### 1.3 添加自定义域名

1. 在域名页面，点击 **"Add Domain"**（添加域名）按钮
2. 输入你的域名：
   - **根域名**：例如 `acgmbti.online`
   - **或者 www 子域名**：`www.acgmbti.online`
   - **推荐**：两个都添加（根域名 + www）
3. 点击 **"Add"** 按钮

#### 1.4 查看并复制 DNS 配置信息 ⚠️ 重要

添加域名后，Vercel 会显示需要配置的 DNS 记录。**请仔细查看并复制这些信息！**

**通常会有两种配置方式：**

**方式 A：CNAME 记录（推荐，简单）**

Vercel 会显示类似这样的信息：
```
类型：CNAME
名称：@ 或 www
值：cname.vercel-dns.com
```

**方式 B：A 记录（根域名必需）**

如果使用根域名，可能需要添加 A 记录：
```
类型：A
名称：@
值：76.76.21.21
（可能有多个 IP，需要都添加）
```

**📋 请记录以下信息（后面会用到）：**
- ✅ 根域名（如 `acgmbti.online`）需要什么类型的记录？A 还是 CNAME？
- ✅ 记录值是什么？（IP 地址或 CNAME 地址）
- ✅ www 子域名（如 `www.acgmbti.online`）需要什么类型的记录？
- ✅ 记录值是什么？

---

### 📝 第二步：在 Spaceship 配置 DNS 记录

#### 2.1 登录 Spaceship 控制台

1. 访问：https://www.spaceship.com/
2. 点击右上角 **"Sign In"** 登录你的账号

#### 2.2 进入域名管理

1. 登录后，在控制面板中找到 **"Domains"** 或 **"My Domains"**
2. 在域名列表中，找到你的域名（例如：`acgmbti.online`）
3. 点击域名进入管理页面

#### 2.3 找到 DNS 管理页面

1. 在域名管理页面，找到 **"DNS Management"** 或 **"DNS Records"** 或 **"DNS 设置"** 选项
2. 点击进入 DNS 设置页面
3. 确认页面显示 **"使用 Spaceship 默认名称服务器"** 或类似提示

#### 2.4 添加 DNS 记录

**情况 1：配置 www 子域名（推荐，最简单）**

1. 点击 **"Add Record"** 或 **"添加记录"** 或 **"+"** 按钮
2. 配置参数：
   - **Type（记录类型）**：选择 **"CNAME"**
   - **Host（主机名）** 或 **Name（名称）**：输入 `www`
   - **Value（值）** 或 **Target（目标）**：粘贴 Vercel 提供的 CNAME 值
     - 例如：`cname.vercel-dns.com`
     - ⚠️ **不要带** `http://` 或 `https://`，只输入域名本身
   - **TTL**：选择 **"600"**（10 分钟）或保持默认值 **"Auto"**
3. 点击 **"Save"** 或 **"保存"** 或 **"Add"** 按钮

**情况 2：配置根域名（例如 `acgmbti.online`）**

**如果 Vercel 要求使用 A 记录（根域名通常需要 A 记录）：**

1. 点击 **"Add Record"** 按钮
2. 配置参数：
   - **Type（记录类型）**：选择 **"A"**
   - **Host（主机名）** 或 **Name（名称）**：输入 `@` 或留空（表示根域名）
   - **Value（值）** 或 **IPv4 address（IPv4 地址）**：粘贴 Vercel 提供的 IP 地址
     - 例如：`76.76.21.21`
     - 如果有多个 IP，需要添加多条 A 记录
   - **TTL**：选择 **"600"**（10 分钟）或 **"Auto"**
3. 点击 **"Save"** 保存
4. **如果有多个 IP**，重复步骤 1-3，为每个 IP 添加一条 A 记录

**如果 Vercel 允许根域名使用 CNAME：**

1. 点击 **"Add Record"** 按钮
2. 配置参数：
   - **Type**：选择 **"CNAME"**
   - **Host**：输入 `@` 或留空
   - **Value**：粘贴 Vercel 提供的 CNAME 值
   - **TTL**：600 或 Auto
3. 点击 **"Save"** 保存

#### 2.5 同时配置根域名和 www（推荐）

如果你想同时支持：
- `acgmbti.online`（根域名）
- `www.acgmbti.online`（www 子域名）

**步骤：**

1. **在 Vercel 中添加两个域名**（如果还没添加）：
   - `acgmbti.online`
   - `www.acgmbti.online`

2. **在 Spaceship DNS 中添加两条记录**：

   **记录 1（根域名）：**
   - Type: **A**（或 CNAME，根据 Vercel 提示）
   - Host: **@** 或留空
   - Value: Vercel 提供的 IP 地址（如 `76.76.21.21`）或 CNAME 值
   - TTL: 600 或 Auto

   **记录 2（www 子域名）：**
   - Type: **CNAME**
   - Host: **www**
   - Value: Vercel 提供的 CNAME 值（如 `cname.vercel-dns.com`）
   - TTL: 600 或 Auto

3. **保存所有记录**

#### 2.6 验证 DNS 记录

保存后，检查 DNS 记录列表，确认记录已添加：
- ✅ 根域名记录（A 或 CNAME）
- ✅ www 子域名记录（CNAME）
- ✅ 记录值正确（不要带协议前缀）
- ✅ **重要**：确保没有启用代理（Proxy）功能

**📝 Spaceship DNS 配置示例：**

```
记录 1（根域名）：
Type: A
Host: @
Value: 76.76.21.21
TTL: 600
Proxy: 关闭（如果有这个选项）

记录 2（www 子域名）：
Type: CNAME
Host: www
Value: cname.vercel-dns.com
TTL: 600
Proxy: 关闭（如果有这个选项）
```

#### 2.7 处理 "Proxy Detected" 警告

如果在 Vercel 控制台看到 **"Proxy Detected"** 警告，说明 DNS 记录可能启用了代理服务。

**为什么会有这个警告？**
- Vercel 检测到你的 DNS 记录可能启用了代理（如 Cloudflare 的代理服务）
- 代理服务会阻止 Vercel 的 DDoS 防护和性能优化工具正常工作
- 可能影响网站性能和安全性

**如何解决？**

**方法 1：检查 Spaceship DNS 记录（推荐）**
1. 回到 Spaceship DNS 管理页面
2. 检查你的两条 DNS 记录（`@` 和 `www`）
3. 如果看到云朵图标（可能是橙色或彩色），点击它关闭代理
   - 云朵图标应该是灰色或关闭状态
   - 如果显示为橙色/彩色，说明代理已启用，需要关闭
4. 保存更改
5. 等待几分钟，然后在 Vercel 点击 "Refresh" 按钮

**方法 2：使用 Vercel 的 "1-click fix" 功能（跳转到 Cloudflare）**

如果点击 "1-click fix" 后跳转到 Cloudflare 控制台，说明域名使用的是 Cloudflare 的名称服务器。需要手动在 Cloudflare 中关闭代理：

**步骤：**

1. **点击域名进入详情**
   - 在 Cloudflare 控制台的域名列表中，点击 `acgmbti.online`（蓝色链接）
   - 进入域名管理页面

2. **找到 DNS 记录设置**
   - 在左侧菜单中，点击 **"DNS"** 或 **"DNS Records"**
   - 或者直接查看页面中的 DNS 记录列表

3. **关闭代理（Proxy）**
   - 找到指向 Vercel 的记录（A 记录或 CNAME 记录）
   - 查看记录右侧的 **云朵图标**：
     - **橙色云朵** = 代理已启用（需要关闭）
     - **灰色云朵** = 代理已关闭（正确状态）
   - 点击云朵图标，将其切换为 **灰色**（关闭代理）

4. **需要关闭的记录**
   - 如果配置了根域名（`@`），关闭根域名记录的代理
   - 如果配置了 www 子域名（`www`），关闭 www 记录的代理
   - 确保所有指向 Vercel 的记录都关闭了代理

5. **保存并验证**
   - 更改会自动保存（Cloudflare 是实时保存的）
   - 等待 5-10 分钟
   - 回到 Vercel 控制台，点击 "Refresh" 按钮
   - 警告应该消失，显示 "Valid Configuration" ✅

**Cloudflare DNS 记录示例：**
```
记录 1（根域名）：
Type: A
Name: @
IPv4 address: 76.76.21.21
Proxy status: 灰色云朵（关闭）✅

记录 2（www 子域名）：
Type: CNAME
Name: www
Target: cname.vercel-dns.com
Proxy status: 灰色云朵（关闭）✅
```

**方法 3：检查是否使用了 Cloudflare**
- 如果域名的名称服务器指向 Cloudflare（如 `gail.ns.cloudflare.com`），需要在 Cloudflare 中关闭代理
- 如果使用 Spaceship 默认名称服务器，通常不会有代理问题

**验证代理是否已关闭：**
- 在 Cloudflare DNS 记录中，云朵图标应该是灰色（关闭状态）
- 在 Spaceship DNS 记录中，不应该看到彩色的云朵图标
- 在 Vercel 控制台，警告应该消失，显示 "Valid Configuration"

---

### 📝 第三步：等待 DNS 解析生效

#### 3.1 等待时间

- **DNS 解析生效时间**：通常需要 **10 分钟到 2 小时**，最长不超过 48 小时
- **不同地区生效时间可能不同**

#### 3.2 验证 DNS 解析

在配置 DNS 后，可以测试解析是否生效：

**Windows PowerShell：**
```powershell
# 测试域名解析
nslookup acgmbti.online

# 或使用 ping
ping acgmbti.online
```

**Mac/Linux：**
```bash
# 使用 dig 命令
dig acgmbti.online

# 或使用 ping
ping acgmbti.online
```

**在线工具：**
- https://dnschecker.org/ - 输入域名，查看全球 DNS 解析状态
- https://www.whatsmydns.net/ - 检查 DNS 解析

#### 3.3 检查 Vercel 域名状态

1. 回到 Vercel 控制台 → 项目 → Settings → Domains
2. 查看你添加的域名状态：
   - **"Valid Configuration"**（配置有效）✅
   - **"Validating"**（验证中）⏳
   - **"Invalid Configuration"**（配置无效）❌

---

### 📝 第四步：等待 HTTPS 证书配置

#### 4.1 自动配置

- Vercel 会自动为你的域名申请 **SSL 证书**（Let's Encrypt）
- 通常在 DNS 解析生效后 **5-10 分钟**完成
- 无需手动操作

#### 4.2 检查证书状态

1. 在 Vercel → Settings → Domains
2. 查看域名状态：
   - 如果显示 **"Valid"**（有效）✅，说明配置成功
   - 如果显示 **"Validating"**（验证中）⏳，需要等待

---

### 📝 第五步：测试访问

#### 5.1 访问域名

等待 DNS 和 HTTPS 配置完成后（通常 10-30 分钟），在浏览器中访问：

```
https://acgmbti.online
```

或（如果配置了 www）：

```
https://www.acgmbti.online
```

#### 5.2 验证功能

- ✅ 页面正常加载
- ✅ 浏览器地址栏显示锁图标（🔒）表示 HTTPS 正常
- ✅ 前端路由正常工作（刷新页面不会 404）
- ✅ API 请求正常（检查浏览器 Network 标签）

#### 5.3 如果访问失败

**问题 1：DNS 解析未生效**
- 等待更长时间（最长 48 小时）
- 清除本地 DNS 缓存：
  ```powershell
  # Windows
  ipconfig /flushdns
  ```

**问题 2：HTTPS 证书未配置完成**
- 等待 5-10 分钟
- 检查 Vercel 域名状态

**问题 3：DNS 配置错误**
- 检查 DNS 记录是否正确
- 确认记录值是否正确（不要带协议前缀）
- 确认记录类型是否正确（A 或 CNAME）

---

### 🔧 常见问题

#### Q1: 根域名和 www 子域名可以同时使用吗？

**A:** 可以！在 Vercel 中添加两个域名，然后分别配置 DNS 解析：
- `acgmbti.online`：使用 A 记录或 CNAME（根据 Vercel 提示）
- `www.acgmbti.online`：使用 CNAME 记录

#### Q2: 配置后多久可以访问？

**A:** 
- DNS 解析：10 分钟到 2 小时
- HTTPS 证书：5-10 分钟
- **总计：通常 15-30 分钟**，最长不超过 48 小时

#### Q3: 需要备案吗？

**A:** 
- 如果只使用 Vercel（国外服务器），**通常不需要备案**
- 如果未来迁移到国内平台（如阿里云 OSS），需要备案

#### Q4: 访问速度慢怎么办？

**A:** 
- Vercel 服务器在国外，中国大陆用户访问可能较慢
- 如果速度慢，可以考虑：
  1. 使用 VPN 加速
  2. 迁移到国内平台（阿里云 OSS + CDN）

#### Q5: 如何更新代码？

**A:** 
- 推送代码到 GitHub，Vercel 会自动重新部署
- 自定义域名也会自动更新

---

### ✅ 配置检查清单

- [ ] 已在 Vercel 添加域名（根域名 + www）
- [ ] 已复制 Vercel 显示的 DNS 配置信息
- [ ] 已在 Spaceship 添加根域名 DNS 记录（A 或 CNAME）
- [ ] 已在 Spaceship 添加 www 子域名 DNS 记录（CNAME）
- [ ] DNS 解析已生效（可以 ping 通或 nslookup 成功）
- [ ] Vercel 显示域名状态为 "Valid"（有效）
- [ ] HTTPS 证书已配置（浏览器显示锁图标）
- [ ] 网站可以正常访问 `https://你的域名`
- [ ] 所有功能正常工作

---

### 📞 需要帮助？

1. **Vercel 官方文档**
   - 自定义域名：https://vercel.com/docs/concepts/projects/domains
   - 支持：https://vercel.com/support

2. **Spaceship 帮助**
   - 帮助中心：https://www.spaceship.com/help/
   - 客服支持：在 Spaceship 控制台查找 "Support" 或 "帮助"

3. **项目文档**
   - 详细部署指南：`VERCEL_CUSTOM_DOMAIN.md`
   - 国内部署指南：`FRONTEND_DEPLOY_GUIDE.md`

---

**配置完成后，你的网站就可以通过自定义域名访问了！** 🎉

如果遇到问题，告诉我你的域名和具体错误信息，我可以帮你排查！
